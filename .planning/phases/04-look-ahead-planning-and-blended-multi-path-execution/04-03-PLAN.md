---
plan_id: 04-03
phase: 4
title: Controller Wiring — _execute_callback Generator Loop and cancel_callback
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/movement_controller/movement_controller/ur_movement_controller.py
autonomous: true
requirements: [MOT-02, MOT-03, MOT-04]
status: pending

must_haves:
  truths:
    - "_execute_callback calls plan_all(groups) once then drives execution via iterate_planned_trajectories()"
    - "Feedback is group-level: one 'executing' and one 'completed' message per PlanResultDTO (D-01)"
    - "completed_ids is a flat list extended with plan_dto.path_ids after each group (D-02)"
    - "Cancel check (goal_handle.is_cancel_requested) runs at the start of every for-loop iteration"
    - "Plan failure (plan_dto.success=False) triggers goal_handle.abort() with plan_dto.error_message"
    - "Execution uses TrajectoryExecutionManager.push() + execute_and_wait() (not moveit.execute)"
    - "_cancel_callback calls planner_service.cancel() and returns CancelResponse.ACCEPT immediately (D-10)"
    - "ActionServer in on_configure passes cancel_callback=self._cancel_callback"
  artifacts:
    - path: "src/movement_controller/movement_controller/ur_movement_controller.py"
      provides: "Updated _execute_callback with generator loop; _cancel_callback method; updated ActionServer"
      contains: "_cancel_callback"
    - path: "src/movement_controller/movement_controller/ur_movement_controller.py"
      provides: "CancelResponse import and RobotTrajectory import"
      contains: "CancelResponse"
  key_links:
    - from: "_execute_callback"
      to: "PilzPlannerService.plan_all"
      via: "self._planner_service.plan_all(groups)"
      pattern: "plan_all.*groups"
    - from: "_execute_callback"
      to: "iterate_planned_trajectories"
      via: "for plan_dto in self._planner_service.iterate_planned_trajectories()"
      pattern: "iterate_planned_trajectories"
    - from: "_cancel_callback"
      to: "PilzPlannerService.cancel"
      via: "self._planner_service.cancel()"
      pattern: "_planner_service.cancel"
    - from: "on_configure"
      to: "_cancel_callback"
      via: "ActionServer(cancel_callback=self._cancel_callback)"
      pattern: "cancel_callback.*_cancel_callback"
---

<objective>
Replace the Phase 3 per-path execution loop in URMovementController._execute_callback
with a generator-driven group-level loop that consumes PlanResultDTOs from
PilzPlannerService.iterate_planned_trajectories(), executes them via
TrajectoryExecutionManager, and publishes group-level feedback per D-01.

Add _cancel_callback to the ActionServer (D-10) for non-blocking cancellation.

Purpose: Wires the look-ahead thread (Plan 04-02) into the live execution path.
Output: Updated ur_movement_controller.py with the complete Phase 4 execute loop.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-look-ahead-planning-and-blended-multi-path-execution/04-CONTEXT.md
@.planning/phases/04-look-ahead-planning-and-blended-multi-path-execution/04-RESEARCH.md
@src/movement_controller/movement_controller/ur_movement_controller.py

<interfaces>
<!-- Current _execute_callback (to be replaced) — Phase 3 per-path loop -->
<!-- Lines ~155-230 in ur_movement_controller.py  -->
async def _execute_callback(self, goal_handle: ServerGoalHandle) -> ExecuteTrajectory.Result:
    try:
        goal_dto = TrajectoryGoalDTO.from_ros_msg(goal_handle.request)
        groups = TrajectoryGrouper.group(goal_dto.paths)
        completed_ids: list[str] = []

        if self._planner_service is None or self._moveit is None: ...

        for group in groups:           # ← REMOVE this nested loop
            for path in group:
                # per-path feedback, plan, execute
                ...

        result = ExecuteTrajectory.Result()
        result.success = True
        ...
    except Exception as e: ...
    finally:
        with self._executing_lock: self._is_executing = False

<!-- Current ActionServer creation (on_configure ~line 118) -->
self._action_server = ActionServer(
    self, ExecuteTrajectory, "movement_controller/execute_trajectory",
    execute_callback=self._execute_callback,
    goal_callback=self._goal_callback,
    callback_group=ReentrantCallbackGroup(),
)

<!-- New PilzPlannerService API (from Plans 04-01, 04-02) -->
self._planner_service.plan_all(groups: list[list[TrajectoryPathDTO]]) -> None
self._planner_service.iterate_planned_trajectories() -> Iterator[PlanResultDTO]
self._planner_service.cancel() -> None

<!-- PlanResultDTO fields relevant to execution -->
plan_dto.success: bool
plan_dto.error_message: str
plan_dto.path_ids: list[str]   ← used for feedback (D-01) and completed_ids (D-02)
plan_dto.blended: bool
plan_dto.trajectories: list[moveit_msgs.msg.RobotTrajectory]  ← execute each

<!-- TrajectoryExecutionManager usage (Research Finding 7) -->
# robot_model = self._moveit.get_robot_model()
# tem = self._moveit.get_trajectory_execution_manager()
# with self._moveit.get_planning_scene_monitor().read_only() as scene:
#     ref_state = scene.current_state  # moveit.core.robot_state.RobotState
# for ros_traj_msg in plan_dto.trajectories:
#     traj = RobotTrajectory(robot_model)
#     traj.set_robot_trajectory_msg(ref_state, ros_traj_msg)
#     tem.push(traj)
# tem.execute_and_wait()

<!-- cancel_callback signature (Research Finding 8) -->
# def _cancel_callback(self, cancel_request) -> CancelResponse:
#     ...
#     return CancelResponse.ACCEPT
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace _execute_callback with generator-loop execution</name>
  <files>src/movement_controller/movement_controller/ur_movement_controller.py</files>
  <read_first>
    - src/movement_controller/movement_controller/ur_movement_controller.py (full _execute_callback body — understand what to remove and what to keep)
    - .planning/phases/04-look-ahead-planning-and-blended-multi-path-execution/04-RESEARCH.md (Architecture Pattern 3 — complete execute loop pattern; Common Pitfall 8 — TEM behaviour in simulation)
  </read_first>
  <action>
    === IMPORTS ===
    Add to the import line `from rclpy.action import ActionServer, GoalResponse`:
      → change to `from rclpy.action import ActionServer, CancelResponse, GoalResponse`

    Add new import after the moveit imports block:
      from moveit.core.robot_trajectory import RobotTrajectory

    === _execute_callback BODY REPLACEMENT ===
    Keep the outer try/except/finally structure unchanged (the finally block that resets
    _is_executing is critical and must not be removed).

    Keep at the start of the try block (unchanged):
      goal_dto = TrajectoryGoalDTO.from_ros_msg(goal_handle.request)
      groups = TrajectoryGrouper.group(goal_dto.paths)
      completed_ids: list[str] = []
      if self._planner_service is None or self._moveit is None: ...  (keep the guard block)

    REMOVE the Phase 3 nested for loop:
      for group in groups:
          for path in group:
              ...

    REPLACE with the following sequence inside the try block, after the None-guard block:

    1. Acquire TEM and robot_model once before the generator loop (efficient, not per-group):
         robot_model = self._moveit.get_robot_model()
         tem = self._moveit.get_trajectory_execution_manager()

    2. Start look-ahead background thread (D-04):
         self._planner_service.plan_all(groups)

    3. Generator loop — one iteration per planned group:
         for plan_dto in self._planner_service.iterate_planned_trajectories():

         At the start of each iteration, in this order:

         a. Cancel check (before executing anything in this group):
              if goal_handle.is_cancel_requested:
                  self._planner_service.cancel()   # idempotent; may already be called by cancel_callback
                  result = ExecuteTrajectory.Result()
                  result.success = False
                  result.error_message = 'Goal was canceled'
                  result.trajectory_paths_completed = completed_ids  # D-02: partial list
                  try:
                      goal_handle.canceled()
                  except Exception:
                      pass
                  return result

         b. Active-state check:
              if not self._is_active:
                  (abort with 'Node deactivated during execution', same pattern as Phase 3)

         c. Planning failure check (plan_dto.success=False means background planning failed):
              if not plan_dto.success:
                  self.get_logger().error(f'Look-ahead planning failed: {plan_dto.error_message}')
                  result = ExecuteTrajectory.Result()
                  result.success = False
                  result.error_message = plan_dto.error_message
                  result.trajectory_paths_completed = completed_ids  # D-02: partial list
                  try:
                      goal_handle.abort()
                  except Exception:
                      pass
                  return result

         d. Publish group-level 'executing' feedback (D-01):
              fb = ExecuteTrajectory.Feedback()
              fb.status = FeedbackStatusEnum.EXECUTING.value
              fb.trajectory_path_ids = plan_dto.path_ids
              goal_handle.publish_feedback(fb)

         e. Get reference robot state for trajectory conversion:
              with self._moveit.get_planning_scene_monitor().read_only() as scene:
                  ref_state = scene.current_state

         f. Execute all segments in this group via TEM:
              for ros_traj_msg in plan_dto.trajectories:
                  traj = RobotTrajectory(robot_model)
                  traj.set_robot_trajectory_msg(ref_state, ros_traj_msg)
                  tem.push(traj)
              try:
                  tem.execute_and_wait()
              except Exception as exec_err:
                  err_msg = f'TEM execution failed for paths {plan_dto.path_ids}: {exec_err}'
                  self.get_logger().error(err_msg)
                  result = ExecuteTrajectory.Result()
                  result.success = False
                  result.error_message = err_msg
                  result.trajectory_paths_completed = completed_ids
                  try:
                      goal_handle.abort()
                  except Exception:
                      pass
                  return result

         g. Publish group-level 'completed' feedback (D-01):
              fb2 = ExecuteTrajectory.Feedback()
              fb2.status = FeedbackStatusEnum.COMPLETED.value
              fb2.trajectory_path_ids = plan_dto.path_ids
              goal_handle.publish_feedback(fb2)

         h. Track completed IDs (D-02):
              completed_ids.extend(plan_dto.path_ids)

    4. After the generator loop (all groups succeeded):
         result = ExecuteTrajectory.Result()
         result.success = True
         result.error_message = ''
         result.trajectory_paths_completed = completed_ids
         goal_handle.succeed()
         return result

    The except Exception and finally blocks are kept EXACTLY as in Phase 3.
    The `self._moveit.execute(...)` call is fully removed — all execution now goes through TEM.
  </action>
  <verify>
    <automated>cd /workspaces/movement_controller && source /opt/ros/jazzy/setup.bash && source install/setup.bash && python -c "
from movement_controller.ur_movement_controller import URMovementController
import inspect
src = inspect.getsource(URMovementController._execute_callback)
assert 'plan_all' in src, 'plan_all missing from _execute_callback'
assert 'iterate_planned_trajectories' in src, 'iterate_planned_trajectories missing'
assert 'tem.execute_and_wait' in src, 'execute_and_wait missing'
assert 'plan_dto.path_ids' in src, 'group-level path_ids missing'
assert 'moveit.execute' not in src, 'old moveit.execute still present'
print('OK')
"</automated>
  </verify>
  <done>
    _execute_callback contains plan_all, iterate_planned_trajectories, tem.execute_and_wait,
    plan_dto.path_ids references, and does NOT contain self._moveit.execute() or the old
    per-path nested for loop.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add _cancel_callback method and wire into ActionServer (D-10)</name>
  <files>src/movement_controller/movement_controller/ur_movement_controller.py</files>
  <read_first>
    - src/movement_controller/movement_controller/ur_movement_controller.py (on_configure ActionServer block; existing _goal_callback for reference)
    - .planning/phases/04-look-ahead-planning-and-blended-multi-path-execution/04-RESEARCH.md (Finding 8 — cancel_callback signature and CancelResponse; D-10 — non-blocking requirement)
  </read_first>
  <action>
    === ADD _cancel_callback METHOD ===
    Add this method in the "# region: action server callbacks" section, after _goal_callback:

      def _cancel_callback(self, cancel_request) -> CancelResponse:
          """Non-blocking cancel: signal planner to stop and return ACCEPT immediately (D-10).

          Does NOT join the planning thread. Does NOT call moveit.stop().
          The background thread terminates when it next checks cancel_event.
          The iterate_planned_trajectories() generator terminates via the StopIteration sentinel.
          """
          if self._planner_service is not None:
              self._planner_service.cancel()
          return CancelResponse.ACCEPT

    This method MUST NOT:
    - Call self._planning_thread.join()
    - Block on any lock or condition variable
    - Call self._moveit.stop() or any equivalent

    === UPDATE ActionServer IN on_configure ===
    Find the ActionServer construction in on_configure (currently 5 keyword args).
    Add cancel_callback=self._cancel_callback as a new keyword argument:

      self._action_server = ActionServer(
          self,
          ExecuteTrajectory,
          "movement_controller/execute_trajectory",
          execute_callback=self._execute_callback,
          goal_callback=self._goal_callback,
          cancel_callback=self._cancel_callback,   ← ADD THIS LINE
          callback_group=ReentrantCallbackGroup(),
      )
  </action>
  <verify>
    <automated>cd /workspaces/movement_controller && source /opt/ros/jazzy/setup.bash && source install/setup.bash && python -c "
from movement_controller.ur_movement_controller import URMovementController
from rclpy.action import CancelResponse
import inspect

# Verify _cancel_callback exists and returns CancelResponse
src = inspect.getsource(URMovementController._cancel_callback)
assert 'CancelResponse.ACCEPT' in src, 'CancelResponse.ACCEPT missing'
assert 'planner_service.cancel()' in src or '_planner_service.cancel()' in src, 'cancel() call missing'
print('OK — _cancel_callback verified')

on_conf_src = inspect.getsource(URMovementController.on_configure)
assert 'cancel_callback' in on_conf_src, 'cancel_callback not wired into ActionServer'
print('OK — ActionServer has cancel_callback')
"</automated>
  </verify>
  <done>
    _cancel_callback method exists, contains CancelResponse.ACCEPT and planner_service.cancel() call.
    ActionServer in on_configure has cancel_callback=self._cancel_callback argument.
    grep -n 'CancelResponse' src/movement_controller/movement_controller/ur_movement_controller.py → ≥2 hits (import + return).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS2 action goal → _execute_callback | Goal already validated by Pydantic in _goal_callback (Phase 3, unchanged) |
| _cancel_callback → PilzPlannerService.cancel | Internal: no external input processed in cancel path |
| TEM → robot controllers | External: hardware or fake_hardware; exceptions caught and abort issued |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Denial of Service | cancel() called before plan_all() — _plan_queue is None | mitigate | cancel() guards with `if self._plan_queue is not None` (implemented in Plan 04-02) |
| T-04-03-02 | Elevation of Privilege | _cancel_callback could be used to abort goals it didn't originate | accept | ROS2 action server handles cancel authorization; cancel_request contains goal_id; this is standard ROS2 behavior |
| T-04-03-03 | Tampering | tem.execute_and_wait() can receive malformed trajectories from /plan_sequence_path | mitigate | Response validated by MoveItErrorCodes.SUCCESS check in _plan_group_sequence; additional try/except around execute_and_wait catches TEM errors |
| T-04-03-SC | Tampering | No new packages installed | accept | RobotTrajectory, CancelResponse are ROS2 Jazzy built-ins |
</threat_model>

<verification>
Import and structural check:
```bash
cd /workspaces/movement_controller
source /opt/ros/jazzy/setup.bash && source install/setup.bash
python -c "from movement_controller.ur_movement_controller import URMovementController; print('import OK')"
```

Grep checks:
```bash
grep -n 'CancelResponse' src/movement_controller/movement_controller/ur_movement_controller.py
grep -n 'plan_all\|iterate_planned_trajectories\|execute_and_wait' src/movement_controller/movement_controller/ur_movement_controller.py
grep -n 'cancel_callback.*_cancel_callback' src/movement_controller/movement_controller/ur_movement_controller.py
grep -n 'moveit\.execute' src/movement_controller/movement_controller/ur_movement_controller.py
```
Last grep must return 0 lines (old execute() call fully removed).
</verification>

<success_criteria>
- URMovementController imports cleanly with no ImportError
- _execute_callback contains plan_all, iterate_planned_trajectories, execute_and_wait, plan_dto.path_ids
- _execute_callback does NOT contain self._moveit.execute() or the Phase 3 per-path nested loop
- _cancel_callback returns CancelResponse.ACCEPT and calls planner_service.cancel()
- ActionServer in on_configure includes cancel_callback=self._cancel_callback
- Group-level feedback uses plan_dto.path_ids (list), not a single path.path_id string
</success_criteria>

<output>
Create .planning/phases/04-look-ahead-planning-and-blended-multi-path-execution/04-03-SUMMARY.md when done
</output>
