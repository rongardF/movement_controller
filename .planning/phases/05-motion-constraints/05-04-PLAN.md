---
phase: 5
plan: 05-04
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - src/movement_controller/tests/unit/test_constraint_config_dto.py
  - src/movement_controller/tests/unit/test_ur_movement_controller.py
  - src/movement_controller/tests/unit/test_pilz_planner_service.py
autonomous: true
requirements:
  - CON-01
  - CON-02
  - CON-03
  - CON-04
  - CON-05
  - CON-06

must_haves:
  truths:
    - "New test_constraint_config_dto.py covers workspace_enabled, Pydantic validation, BOX geometry, JointConstraint midpoint, OrientationConstraint, and CIRC merge"
    - "test_ur_movement_controller.py covers speed cap rejection and acceleration cap rejection"
    - "test_pilz_planner_service.py covers set_constraints storage, constraint injection into every MotionSequenceItem, and velocity warning"
    - "All new tests pass: pytest exits 0"
  artifacts:
    - path: "src/movement_controller/tests/unit/test_constraint_config_dto.py"
      provides: "constraint config and constraint building unit tests"
    - path: "src/movement_controller/tests/unit/test_ur_movement_controller.py"
      provides: "speed cap rejection tests"
    - path: "src/movement_controller/tests/unit/test_pilz_planner_service.py"
      provides: "set_constraints and injection tests"
  key_links:
    - from: "test_constraint_config_dto.py"
      to: "ConstraintConfigDTO, PilzPlannerService._build_path_constraints, _merge_circ_and_path_constraints"
      via: "direct instantiation + method calls"
      pattern: "_build_path_constraints"
    - from: "test_ur_movement_controller.py speed tests"
      to: "URMovementController._goal_callback"
      via: "node._constraint_config set directly; _goal_callback called with mocked goal"
      pattern: "max_cartesian_speed"
---

## Objective

Write unit tests for all Phase 5 behaviour:

1. **New `test_constraint_config_dto.py`** — ConstraintConfigDTO validation, `workspace_enabled`,
   `_build_path_constraints()` geometry, JointConstraint midpoint, OrientationConstraint fields,
   CIRC merge correctness.
2. **Extended `test_ur_movement_controller.py`** — speed cap and acceleration cap rejection in
   `_goal_callback`.
3. **Extended `test_pilz_planner_service.py`** — `set_constraints()` stores dto, constraints
   injected into every `MotionSequenceItem`, velocity warning logged.

Purpose: Automated verification that CON-01 through CON-06 constraint logic is correct without
requiring a running ROS2 environment.

## Execution Context

@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md

## Context

@.planning/phases/05-motion-constraints/05-CONTEXT.md
@.planning/phases/05-motion-constraints/05-RESEARCH.md
@.planning/phases/05-motion-constraints/05-01-SUMMARY.md
@.planning/phases/05-motion-constraints/05-02-SUMMARY.md
@.planning/phases/05-motion-constraints/05-03-SUMMARY.md

<interfaces>
<!-- Contracts needed by the test author -->

ConstraintConfigDTO (from 05-01):
  Properties: workspace_enabled, joint_constraints_enabled, orientation_constraint_enabled
  Validators: x_min <= x_max; joint arrays same length; joint_max_velocities same length as names

PilzPlannerService (from 05-02):
  set_constraints(dto: ConstraintConfigDTO) -> None   # stores in _constraint_config; warns if max_velocities
  _build_path_constraints(tool_frame: str) -> Constraints
  _merge_circ_and_path_constraints(circ: Constraints, path: Constraints) -> Constraints
  _generate_motion_sequence_request(group, start_state) -> MotionSequenceRequest

URMovementController (from 05-01 + 05-03):
  _constraint_config: ConstraintConfigDTO | None  # can be set directly in tests
  _goal_callback(goal) -> GoalResponse             # checks speed cap when _constraint_config is set

Existing test infrastructure patterns:
  test_pilz_planner_service.py: _build_service() builds activated service with mock node+clients
  test_ur_movement_controller.py: node fixture creates URMovementController; _make_ros_goal() helper
  BSD-3-Clause header required on new files
</interfaces>

## Tasks

<task id="T01" type="execute">
  <title>New test_constraint_config_dto.py — DTO validation + constraint building tests</title>
  <files>src/movement_controller/tests/unit/test_constraint_config_dto.py</files>

  <read_first>
    - src/movement_controller/tests/unit/test_pilz_planner_service.py — BSD-3-Clause header, imports pattern, _build_service() helper, test naming convention
    - src/movement_controller/movement_controller/models/constraint_config_dto.py — all fields, validators, properties (created in 05-01)
    - src/movement_controller/movement_controller/services/pilz_planner_service.py — _build_path_constraints, _merge_circ_and_path_constraints (created in 05-02); _build_circ_constraints (existing; needed to build circ fixture for merge test)
    - .planning/phases/05-motion-constraints/05-RESEARCH.md — finding #4 (CIRC merge: arc at [0], BOX at [1]), #5 (BOX full lengths), #7 (orientation parameterization=0), #8 (joint midpoint)
  </read_first>

  <action>
    Create `test_constraint_config_dto.py` with BSD-3-Clause header and module docstring
    "Unit tests for ConstraintConfigDTO and PilzPlannerService constraint-building methods."

    Imports needed:
    - `import math`, `pytest`
    - `from unittest.mock import MagicMock`
    - `from pydantic import ValidationError`
    - `from rclpy.lifecycle import LifecycleNode`
    - `from rclpy.impl.rcutils_logger import RcutilsLogger`
    - `from moveit_msgs.msg import Constraints, JointConstraint`
    - `from shape_msgs.msg import SolidPrimitive`
    - `from movement_controller.models.constraint_config_dto import ConstraintConfigDTO`
    - `from movement_controller.services.pilz_planner_service import PilzPlannerService`
    - Pydantic forward refs rebuild (copy pattern from test_pilz_planner_service.py):
      `PlanResultDTO.model_rebuild(...)` and `PlanningSessionDTO.model_rebuild(...)`

    Add a module-level helper `_make_service(dto=None)` that creates a minimal activated
    `PilzPlannerService` backed by mock node (pattern from `_build_service` in
    test_pilz_planner_service.py — create mock node with MagicMock logger, create the service,
    call `svc.on_activate()`, optionally call `svc.set_constraints(dto)` if dto is not None,
    return svc). No real ROS2 service clients needed since tests only call private methods.

    --- ConstraintConfigDTO tests ---

    `test_workspace_enabled_false_at_sentinel_defaults`:
    `ConstraintConfigDTO()` → `workspace_enabled == False`

    `test_workspace_enabled_true_when_z_narrowed`:
    `ConstraintConfigDTO(z_min=0.0, z_max=0.5)` → `workspace_enabled == True`

    `test_workspace_enabled_true_when_x_narrowed`:
    `ConstraintConfigDTO(x_min=-2.0, x_max=2.0)` → `workspace_enabled == True`

    `test_workspace_disabled_requires_all_axes_at_sentinel`:
    Build dto with y narrowed but x and z at sentinel: `ConstraintConfigDTO(y_min=0.0, y_max=1.0)` →
    `workspace_enabled == True` (only z still at sentinel — y narrows the full range)

    `test_joint_constraints_enabled_false_with_empty_names`:
    `ConstraintConfigDTO()` → `joint_constraints_enabled == False`

    `test_joint_constraints_enabled_true_with_names`:
    `ConstraintConfigDTO(joint_names=['j1'], joint_lower_limits=[-1.0], joint_upper_limits=[1.0])` →
    `joint_constraints_enabled == True`

    `test_orientation_constraint_enabled_false_at_defaults`:
    `ConstraintConfigDTO()` → `orientation_constraint_enabled == False`

    `test_orientation_constraint_enabled_true_when_narrowed`:
    `ConstraintConfigDTO(orientation_tolerance_z=0.1)` → `orientation_constraint_enabled == True`

    `test_validation_error_x_min_greater_than_x_max`:
    `ConstraintConfigDTO(x_min=1.0, x_max=0.0)` → raises `ValidationError`

    `test_validation_error_joint_array_length_mismatch`:
    `ConstraintConfigDTO(joint_names=['j1', 'j2'], joint_lower_limits=[-1.0])` → raises `ValidationError`
    (upper_limits missing + lengths differ)

    `test_validation_error_max_velocities_length_mismatch`:
    `ConstraintConfigDTO(joint_names=['j1'], joint_lower_limits=[-1.0], joint_upper_limits=[1.0], joint_max_velocities=[0.5, 0.5])` → raises `ValidationError`

    --- _build_path_constraints tests ---

    `test_build_path_constraints_returns_empty_when_no_constraint_config`:
    `svc = _make_service()` (no dto set) → `_build_path_constraints('tool0')` returns a `Constraints()` with no position/joint/orientation constraints

    `test_build_path_constraints_returns_empty_when_all_disabled`:
    `svc = _make_service(ConstraintConfigDTO())` → all constraints disabled by sentinels → result has
    empty position_constraints, joint_constraints, orientation_constraints

    `test_build_path_constraints_box_full_lengths`:
    `dto = ConstraintConfigDTO(x_min=-1.0, x_max=2.0, y_min=0.0, y_max=3.0, z_min=0.0, z_max=0.5)`
    `svc = _make_service(dto)` → `c = svc._build_path_constraints('tool0')`
    Assert: `len(c.position_constraints) == 1`
    Assert: `c.position_constraints[0].constraint_region.primitives[0].type == SolidPrimitive.BOX`
    Assert: `c.position_constraints[0].constraint_region.primitives[0].dimensions == [3.0, 3.0, 0.5]`
    (full lengths: 2.0-(-1.0)=3.0, 3.0-0.0=3.0, 0.5-0.0=0.5)
    Assert: `c.position_constraints[0].constraint_region.primitive_poses[0].position.x == pytest.approx(0.5)`
    (center: (-1.0+2.0)/2 = 0.5)

    `test_build_path_constraints_joint_midpoint_and_tolerances`:
    `dto = ConstraintConfigDTO(joint_names=['j1'], joint_lower_limits=[-1.0], joint_upper_limits=[1.0])`
    `c = _make_service(dto)._build_path_constraints('tool0')`
    Assert: `len(c.joint_constraints) == 1`
    Assert: `c.joint_constraints[0].joint_name == 'j1'`
    Assert: `c.joint_constraints[0].position == pytest.approx(0.0)`  (midpoint: (-1+1)/2=0)
    Assert: `c.joint_constraints[0].tolerance_above == pytest.approx(1.0)` (1.0 - 0.0)
    Assert: `c.joint_constraints[0].tolerance_below == pytest.approx(1.0)` (0.0 - (-1.0))

    `test_build_path_constraints_orientation_fields`:
    `dto = ConstraintConfigDTO(orientation_tolerance_x=0.1, orientation_tolerance_y=0.2, orientation_tolerance_z=0.3)`
    `c = _make_service(dto)._build_path_constraints('my_tool')`
    Assert: `len(c.orientation_constraints) == 1`
    Assert: `c.orientation_constraints[0].link_name == 'my_tool'`
    Assert: `c.orientation_constraints[0].absolute_x_axis_tolerance == pytest.approx(0.1)`
    Assert: `c.orientation_constraints[0].absolute_y_axis_tolerance == pytest.approx(0.2)`
    Assert: `c.orientation_constraints[0].absolute_z_axis_tolerance == pytest.approx(0.3)`
    Assert: `c.orientation_constraints[0].parameterization == 0`
    Assert: `c.orientation_constraints[0].orientation.w == pytest.approx(1.0)` (identity quaternion)

    --- _merge_circ_and_path_constraints tests ---

    `test_merge_preserves_circ_arc_at_index_zero`:
    Build a circ Constraints with name='interim' and one position_constraint (arc point).
    Build a path Constraints with one position_constraint (BOX from workspace constraint).
    `merged = svc._merge_circ_and_path_constraints(circ, path)`
    Assert: `merged.name == 'interim'`
    Assert: `len(merged.position_constraints) == 2`
    Assert: `merged.position_constraints[0]` is the arc point (same object reference as circ.position_constraints[0])
    Assert: `merged.position_constraints[1]` is the BOX (same object reference as path.position_constraints[0])

    `test_merge_with_empty_path_constraints`:
    circ has name='center', one arc position_constraint.
    path = `Constraints()` (all empty lists).
    `merged = svc._merge_circ_and_path_constraints(circ, path)`
    Assert: `merged.name == 'center'`
    Assert: `len(merged.position_constraints) == 1` (only arc point, no BOX added)

    For all tests in this file, call `pytest.approx` where float comparisons are needed.
    Helper `_make_service(dto=None)` should NOT call `on_activate()` since no ROS2 clients
    are needed for constraint-building tests — just create `PilzPlannerService(mock_node, 'ur_manipulator')`
    and optionally call `svc.set_constraints(dto)`. If `on_activate()` is needed for proper
    initialization of the _constraint_config attribute, call it after construction.
  </action>

  <acceptance_criteria>
    - Source: `test_constraint_config_dto.py` has BSD-3-Clause license header
    - Source: `test_constraint_config_dto.py` imports `ConstraintConfigDTO` and `PilzPlannerService`
    - Source: file contains at least 15 test functions with names matching `test_*`
    - Test: `python -m pytest src/movement_controller/tests/unit/test_constraint_config_dto.py -v` exits 0
    - Behaviour: `test_build_path_constraints_box_full_lengths` asserts `dimensions == [3.0, 3.0, 0.5]`
    - Behaviour: `test_build_path_constraints_joint_midpoint_and_tolerances` asserts `position == 0.0` and both tolerances == 1.0
    - Behaviour: `test_merge_preserves_circ_arc_at_index_zero` confirms arc at [0] and BOX at [1]
    - Behaviour: `test_validation_error_x_min_greater_than_x_max` uses `pytest.raises(ValidationError)`
  </acceptance_criteria>
</task>

<task id="T02" type="execute">
  <title>Extend test_ur_movement_controller.py + test_pilz_planner_service.py</title>
  <files>
    src/movement_controller/tests/unit/test_ur_movement_controller.py,
    src/movement_controller/tests/unit/test_pilz_planner_service.py
  </files>

  <read_first>
    - src/movement_controller/tests/unit/test_ur_movement_controller.py — full file: `node` fixture (creates URMovementController), `_make_ros_goal()` helper, existing goal rejection tests (pattern to follow); note `_make_ros_goal` sets `cartesian_speed=0.0` — new tests need goals with `cartesian_speed > 0.0`
    - src/movement_controller/tests/unit/test_pilz_planner_service.py — full file: `_build_service()` helper, `_make_path_dto()` helper, existing plan_all tests (pattern for sequence request inspection)
    - src/movement_controller/movement_controller/ur_movement_controller.py — _goal_callback (current state after 05-03) to understand when speed check runs
    - src/movement_controller/movement_controller/services/pilz_planner_service.py — set_constraints, _generate_motion_sequence_request (after 05-02) to understand what to assert on items
  </read_first>

  <action>
    --- test_ur_movement_controller.py additions ---

    Append to the end of the file (after the last existing test). Add import at top if missing:
    `from movement_controller.models.constraint_config_dto import ConstraintConfigDTO`

    `test_goal_rejected_when_cartesian_speed_exceeds_max`:
    Create a fresh `node` (use the `node` fixture); set `node._constraint_config = ConstraintConfigDTO(max_cartesian_speed=0.5)`.
    Build a mock goal where `path.cartesian_speed = 0.8` (exceeds 0.5 cap).
    Use the `_make_ros_goal()` helper but override `mock_path.cartesian_speed = 0.8`.
    Call `node._goal_callback(goal)`.
    Assert result == `GoalResponse.REJECT`.
    Assert `node._is_executing == False` (lock released).

    `test_goal_accepted_when_cartesian_speed_within_max`:
    Same setup but `cartesian_speed = 0.3` (within 0.5 cap).
    Assert result == `GoalResponse.ACCEPT`.
    Reset `node._is_executing = False` after.

    `test_goal_accepted_when_max_cartesian_speed_is_zero`:
    Set `node._constraint_config = ConstraintConfigDTO(max_cartesian_speed=0.0)` (unconstrained).
    Build goal with `cartesian_speed = 999.0`.
    Assert result == `GoalResponse.ACCEPT` (0.0 cap = unconstrained, skip check).
    Reset `node._is_executing = False` after.

    `test_goal_rejected_when_acceleration_exceeds_max`:
    Set `node._constraint_config = ConstraintConfigDTO(max_acceleration=0.3)`.
    Build goal with `mock_path.acceleration = 0.5` (exceeds cap).
    Assert result == `GoalResponse.REJECT`.
    Assert `node._is_executing == False`.

    `test_goal_accepted_when_constraint_config_is_none`:
    Confirm `node._constraint_config is None` (default after __init__).
    Build goal with `cartesian_speed = 999.0`.
    Assert result == `GoalResponse.ACCEPT` (no cap when config not set).
    Reset `node._is_executing = False` after.

    --- test_pilz_planner_service.py additions ---

    Append to the end of the file. Add missing imports at top if needed:
    `from movement_controller.models.constraint_config_dto import ConstraintConfigDTO`
    `from moveit_msgs.msg import JointConstraint`
    `from shape_msgs.msg import SolidPrimitive`

    `test_set_constraints_stores_dto`:
    `svc, _, _ = _build_service()` → `dto = ConstraintConfigDTO()` → `svc.set_constraints(dto)` →
    assert `svc._constraint_config is dto` (same object).

    `test_set_constraints_logs_warning_for_nonempty_max_velocities`:
    `svc, _, _ = _build_service()`
    `dto = ConstraintConfigDTO(joint_names=['j1'], joint_lower_limits=[-1.0], joint_upper_limits=[1.0], joint_max_velocities=[0.5])`
    `svc.set_constraints(dto)`
    Verify logger warning was called: `svc._node.get_logger.return_value.warning.assert_called_once()`.
    The warning call args should contain the string 'max_velocities' or 'D-13'.

    `test_set_constraints_no_warning_when_max_velocities_empty`:
    `svc, _, _ = _build_service()`
    `dto = ConstraintConfigDTO(joint_names=['j1'], joint_lower_limits=[-1.0], joint_upper_limits=[1.0])`
    `svc.set_constraints(dto)` → assert `svc._node.get_logger.return_value.warning.call_count == 0`.

    `test_constraints_injected_into_every_sequence_item`:
    Configure active workspace constraint: `dto = ConstraintConfigDTO(x_min=-1.0, x_max=1.0)`.
    `svc, _, _ = _build_service()` → `svc.set_constraints(dto)`.
    Build a group of TWO path DTOs (both LIN, different UUIDs) using `_make_path_dto()`.
    Call `svc._generate_motion_sequence_request([path1, path2], MagicMock())`.
    Assert: both items have `item.req.path_constraints.position_constraints` non-empty.
    Assert: both items' `path_constraints.position_constraints[0].constraint_region.primitives[0].type == SolidPrimitive.BOX`.

    `test_constraints_not_injected_when_all_disabled`:
    `dto = ConstraintConfigDTO()` (all sentinels = all disabled).
    `svc, _, _ = _build_service()` → `svc.set_constraints(dto)`.
    Build one LIN path; call `_generate_motion_sequence_request([path], MagicMock())`.
    Assert: `len(items[0].req.path_constraints.position_constraints) == 0`
    AND `len(items[0].req.path_constraints.joint_constraints) == 0`
    AND `len(items[0].req.path_constraints.orientation_constraints) == 0`.
  </action>

  <acceptance_criteria>
    - Source: `test_ur_movement_controller.py` contains `test_goal_rejected_when_cartesian_speed_exceeds_max` and `test_goal_rejected_when_acceleration_exceeds_max`
    - Source: `test_pilz_planner_service.py` contains `test_set_constraints_stores_dto`, `test_set_constraints_logs_warning_for_nonempty_max_velocities`, `test_constraints_injected_into_every_sequence_item`
    - Test: `python -m pytest src/movement_controller/tests/unit/test_ur_movement_controller.py -v -k "speed or acceleration or constraint"` exits 0
    - Test: `python -m pytest src/movement_controller/tests/unit/test_pilz_planner_service.py -v -k "constraint or set_constraints or warning"` exits 0
    - Behaviour: `test_goal_rejected_when_cartesian_speed_exceeds_max` → `GoalResponse.REJECT`
    - Behaviour: `test_goal_accepted_when_max_cartesian_speed_is_zero` → `GoalResponse.ACCEPT` (0.0 = unconstrained)
    - Behaviour: `test_constraints_injected_into_every_sequence_item` → both items have non-empty position_constraints with BOX type
  </acceptance_criteria>
</task>

## Verification

```bash
source /opt/ros/jazzy/setup.bash && source install/setup.bash
# Run all new and extended unit tests
python -m pytest src/movement_controller/tests/unit/test_constraint_config_dto.py -v
python -m pytest src/movement_controller/tests/unit/test_ur_movement_controller.py -v
python -m pytest src/movement_controller/tests/unit/test_pilz_planner_service.py -v
# Run all unit tests together (must not regress existing tests)
python -m pytest src/movement_controller/tests/unit/ -v
```

## must_haves

```yaml
must_haves:
  truths:
    - "All new test functions present in their respective files"
    - "All test files exit 0 with python -m pytest"
    - "No existing tests broken (full unit test suite passes)"
  artifacts:
    - path: "src/movement_controller/tests/unit/test_constraint_config_dto.py"
      provides: ">=15 test functions covering ConstraintConfigDTO validation and PilzPlannerService constraint building"
```

## Security

Test files mock all ROS2 infrastructure. No external services called. No risk of injection or
security issues in pure unit tests.

## Success Criteria

- `python -m pytest src/movement_controller/tests/unit/ -v` passes all tests (new + existing)
- BOX full-lengths test asserts correct `dimensions` values
- Joint midpoint test asserts `position == (lower+upper)/2` and non-negative tolerances
- Speed cap tests confirm REJECT when over cap and ACCEPT when under cap or cap=0.0

## Output

Create `.planning/phases/05-motion-constraints/05-04-SUMMARY.md` when done.
