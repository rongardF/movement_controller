---
plan: 2
phase: 1
title: ROS2 Interface Files
wave: 2
depends_on:
  - "01-01"
files_modified:
  - action/ExecuteTrajectory.action
  - msg/TrajectoryPath.msg
  - srv/AddObject.srv
  - srv/AttachObject.srv
  - srv/DetachObject.srv
  - srv/RemoveObject.srv
  - srv/ModifyAcm.srv
requirements:
  - PKG-03
  - PKG-05
  - UR-02
autonomous: true
must_haves:
  truths:
    - All 7 interface files exist in their declared subdirectories (action/, msg/, srv/)
    - ExecuteTrajectory.action has Goal with TrajectoryPath[] paths, Result with success/error_message/trajectory_paths_completed, Feedback with status/trajectory_path_ids
    - TrajectoryPath.msg has all string constants (MOTION_TYPE_LIN/PTP/CIRC, CIRC_TYPE_INTERIM/CENTER) and all fields including tool_frame and circ_point
    - All 5 scene service files have consistent response structure (bool success, string error_message, string object_id)
    - All interface files carry BSD-3-Clause license headers
    - colcon build --symlink-install exits 0 after these files are added
  artifacts:
    - path: action/ExecuteTrajectory.action
      provides: Action interface for trajectory execution with TrajectoryPath[] goal
    - path: msg/TrajectoryPath.msg
      provides: Message type for individual trajectory paths with motion constants and CIRC fields
    - path: srv/AddObject.srv
      provides: Service for adding collision objects (primitive + mesh mode, server-generated UUID4)
    - path: srv/AttachObject.srv
      provides: Service for attaching collision objects to robot links
    - path: srv/DetachObject.srv
      provides: Service for detaching attached objects
    - path: srv/RemoveObject.srv
      provides: Service for removing collision objects from scene
    - path: srv/ModifyAcm.srv
      provides: Service for modifying ACM entries using pair-list encoding
  key_links:
    - from: action/ExecuteTrajectory.action
      to: msg/TrajectoryPath.msg
      via: TrajectoryPath[] paths — same-package type reference (no package prefix)
    - from: msg/TrajectoryPath.msg
      to: geometry_msgs/PoseStamped
      via: target_pose field — declared as geometry_msgs DEPENDENCY in CMakeLists.txt
    - from: srv/AddObject.srv
      to: shape_msgs/SolidPrimitive
      via: primitive field — declared as shape_msgs DEPENDENCY in CMakeLists.txt
---

# Plan 2: ROS2 Interface Files

## Goal

Create all 7 ROS2 interface files (1 action, 1 message, 5 services) with exact field definitions per decisions D-01 through D-13, each carrying a BSD-3-Clause header.

## Tasks

### Task 2-2-1: Create ExecuteTrajectory.action and TrajectoryPath.msg

<wave>2</wave>
<type>execute</type>
<autonomous>true</autonomous>

<read_first>
- `.planning/phases/01-package-scaffold-and-interface-definitions/01-RESEARCH.md` — §Interface Files (Exact Syntax) section contains the verified IDL content for ExecuteTrajectory.action and TrajectoryPath.msg including exact field names, types, and string constant syntax
- `.planning/phases/01-package-scaffold-and-interface-definitions/01-CONTEXT.md` — D-01 (flat CIRC fields), D-02 (string constants), D-03 (circ_point as geometry_msgs/Point), D-04 (single target_pose), D-05 (tool_frame field)
</read_first>

<action>
Create directory `action/` and file `action/ExecuteTrajectory.action`.

Add BSD-3-Clause license comment block at the top (year 2026, "Movement Controller Contributors", using `#` comment style). Then the interface content:

Goal section (before first `---`):
- `TrajectoryPath[] paths` with a comment "# Goal: ordered list of trajectory paths to execute"

Separator: `---`

Result section:
- `bool success`
- `string error_message`
- `string[] trajectory_paths_completed`

Separator: `---`

Feedback section:
- `string status` with comment `# "executing" or "completed"`
- `string[] trajectory_path_ids`

Important: `TrajectoryPath` has no package prefix — it is from the same package. This is the ROS2 same-package convention.

---

Create directory `msg/` and file `msg/TrajectoryPath.msg`.

Add BSD-3-Clause license comment block at top. Then the interface content:

String constants block (per D-02, constants MUST be UPPERCASE):
- `string MOTION_TYPE_LIN="LIN"`
- `string MOTION_TYPE_PTP="PTP"`
- `string MOTION_TYPE_CIRC="CIRC"`
- (blank line)
- `string CIRC_TYPE_INTERIM="interim"`
- `string CIRC_TYPE_CENTER="center"`

Fields block (per D-01 through D-05):
- `string path_id`
- `string motion_type`
- `geometry_msgs/PoseStamped target_pose`
- `float64 blend_radius`
- `float64 cartesian_speed`
- `float64 acceleration`
- `string tool_frame` (per D-05; empty string = use tool0 at plan time)
- `string circ_type` (per D-01/D-02; flat field, not sub-message)
- `geometry_msgs/Point circ_point` (per D-01/D-03; flat field, frame assumed from target_pose header)
</action>

<acceptance_criteria>
- `cat action/ExecuteTrajectory.action | grep "TrajectoryPath\[\] paths"` returns the line
- `grep -c "^---$" action/ExecuteTrajectory.action` outputs `2` (two separators for 3 sections)
- `grep "trajectory_paths_completed" action/ExecuteTrajectory.action` returns the result field line
- `grep "trajectory_path_ids" action/ExecuteTrajectory.action` returns the feedback field line
- `grep "MOTION_TYPE_LIN" msg/TrajectoryPath.msg` returns `string MOTION_TYPE_LIN="LIN"`
- `grep "CIRC_TYPE_INTERIM" msg/TrajectoryPath.msg` returns `string CIRC_TYPE_INTERIM="interim"`
- `grep "circ_point" msg/TrajectoryPath.msg` returns `geometry_msgs/Point circ_point`
- `grep "tool_frame" msg/TrajectoryPath.msg` returns `string tool_frame`
- `grep "geometry_msgs/PoseStamped target_pose" msg/TrajectoryPath.msg` returns the line
- `head -1 action/ExecuteTrajectory.action` starts with `# Copyright`
- `head -1 msg/TrajectoryPath.msg` starts with `# Copyright`
</acceptance_criteria>

---

### Task 2-2-2: Create all 5 scene management service files

<wave>2</wave>
<type>execute</type>
<autonomous>true</autonomous>

<read_first>
- `.planning/phases/01-package-scaffold-and-interface-definitions/01-RESEARCH.md` — §Interface Files (Exact Syntax) contains the verified IDL content for all 5 service files
- `.planning/phases/01-package-scaffold-and-interface-definitions/01-CONTEXT.md` — D-06 (5 separate srv files), D-07 (consistent response: success/error_message/object_id), D-08 (server-generated UUID4 for AddObject), D-09 (SolidPrimitive + mesh_file_path), D-10 (PoseStamped), D-11 (AttachObject fields), D-12 (no auto-ACM), D-13 (pair-list for ModifyAcm)
</read_first>

<action>
Create directory `srv/` and the 5 service files. Each file must start with the BSD-3-Clause license comment block (year 2026, "Movement Controller Contributors").

**srv/AddObject.srv** (per D-08, D-09, D-10):
Request section:
- `shape_msgs/SolidPrimitive primitive` (for primitive geometry — BOX/SPHERE/CYLINDER)
- `geometry_msgs/PoseStamped pose` (includes frame_id in header, per D-10)
- `string mesh_file_path` (non-empty = mesh mode, ignores primitive field; per D-09)
Note: NO `object_id` in request — server generates UUID4 and returns it (per D-08).
Separator: `---`
Response section (per D-07):
- `bool success`
- `string error_message`
- `string object_id` (UUID4 generated by server)

**srv/AttachObject.srv** (per D-11):
Request: `string object_id`, `string link_name`, `geometry_msgs/Pose attach_pose`
Separator: `---`
Response (per D-07): `bool success`, `string error_message`, `string object_id`

**srv/DetachObject.srv**:
Request: `string object_id`
Separator: `---`
Response (per D-07): `bool success`, `string error_message`, `string object_id`

**srv/RemoveObject.srv**:
Request: `string object_id`
Separator: `---`
Response (per D-07): `bool success`, `string error_message`, `string object_id`

**srv/ModifyAcm.srv** (per D-13 pair-list encoding):
Request:
- `string[] object_ids_a`
- `string[] object_ids_b`
- `bool allowed`
Separator: `---`
Response (per D-07): `bool success`, `string error_message`, `string object_id`

Add a comment in ModifyAcm.srv above the request fields: `# Sets ACM entry for every (a_i, b_j) pair across the two lists`
</action>

<acceptance_criteria>
- `ls srv/` contains exactly: `AddObject.srv AttachObject.srv DetachObject.srv RemoveObject.srv ModifyAcm.srv`
- `grep "shape_msgs/SolidPrimitive" srv/AddObject.srv` returns the primitive field line
- `grep "mesh_file_path" srv/AddObject.srv` returns the string field line
- `grep "object_id" srv/AddObject.srv | grep "request\|^string object_id"` does NOT find object_id in the request section (UUID4 is server-generated; only appears in response)
- `grep -c "^---$" srv/AddObject.srv` outputs `1` (one separator for request/response)
- `grep "geometry_msgs/Pose attach_pose" srv/AttachObject.srv` returns the field line
- `grep "string\[\] object_ids_a" srv/ModifyAcm.srv` returns the field line
- `grep "bool allowed" srv/ModifyAcm.srv` returns the field line
- All 5 srv files have `bool success`, `string error_message`, `string object_id` in response:
  `for f in srv/*.srv; do grep -c "string object_id" "$f"; done` — each outputs `1`
- `head -1 srv/AddObject.srv` starts with `# Copyright`
</acceptance_criteria>

---

## Verification

```bash
# Verify all 7 interface files exist:
ls action/ExecuteTrajectory.action msg/TrajectoryPath.msg srv/AddObject.srv srv/AttachObject.srv srv/DetachObject.srv srv/RemoveObject.srv srv/ModifyAcm.srv

# Verify TrajectoryPath string constants are uppercase (ROS2 IDL requirement):
grep "MOTION_TYPE_\|CIRC_TYPE_" msg/TrajectoryPath.msg

# Verify same-package reference (no package prefix on TrajectoryPath):
grep "TrajectoryPath" action/ExecuteTrajectory.action | grep -v "movement_controller/"

# Build to confirm interface files compile without errors:
source /opt/ros/jazzy/setup.bash && colcon build --symlink-install --packages-select movement_controller 2>&1 | tail -20

# Confirm all BSD headers present:
for f in action/*.action msg/*.msg srv/*.srv; do head -1 "$f"; done
```

## Threat Model

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Interface field type mismatch (wrong geometry_msgs type used) | LOW | Fields verified against RESEARCH.md which cross-referenced official ROS2 geometry_msgs / shape_msgs definitions; colcon build will catch type resolution errors at compile time |
| Missing package dependency for interface type (shape_msgs not in DEPENDS) | LOW | CMakeLists.txt Plan 1 already declares `shape_msgs` and `geometry_msgs` in rosidl_generate_interfaces DEPENDENCIES; build fails fast if missing |
| Incorrect string constant syntax breaking Python codegen | LOW | RESEARCH.md verified against official ROS2 About-Interfaces docs; constant names MUST be UPPERCASE; build will fail if syntax is invalid |
| No threats with HIGH or MEDIUM severity | N/A | Interface IDL files contain no executable code, no network calls, no user input |
</threat_model>
