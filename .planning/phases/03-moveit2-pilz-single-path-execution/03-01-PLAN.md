---
phase: "03-moveit2-pilz-single-path-execution"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/Dockerfile
  - src/movement_controller/movement_controller/ur_movement_controller.py
autonomous: true
requirements:
  - MOT-05

must_haves:
  truths:
    - "docker/Dockerfile apt block contains 'ros-jazzy-moveit' on its own line"
    - "URMovementController.__init__ declares self._moveit and self._planner_service (both None initially)"
    - "on_configure declares moveit_connection_timeout (float, default 10.0) via declare_parameter"
    - "on_configure spawns a daemon thread for MoveItPy init; if thread times out it logs ERROR and returns TransitionCallbackReturn.FAILURE"
    - "on_configure on exception logs ERROR and returns TransitionCallbackReturn.FAILURE"
    - "on_configure stores self._moveit and injects (moveit, planning_component) into PilzPlannerService constructor"
    - "on_cleanup sets self._moveit and self._planner_service to None after action server teardown"
  artifacts:
    - path: "docker/Dockerfile"
      provides: "ros-jazzy-moveit apt package installation"
      contains: "ros-jazzy-moveit"
    - path: "src/movement_controller/movement_controller/ur_movement_controller.py"
      provides: "MoveItPy lifecycle integration"
      contains: "moveit_connection_timeout"
  key_links:
    - from: "ur_movement_controller.on_configure"
      to: "PilzPlannerService.__init__"
      via: "constructor injection of (moveit, planning_component)"
      pattern: "PilzPlannerService(self._moveit"
    - from: "ur_movement_controller.on_configure"
      to: "MoveItPy"
      via: "threading.Thread with timeout"
      pattern: "init_thread.join\\(timeout="
---

<objective>
Install the `ros-jazzy-moveit` apt package in the Dockerfile so the devcontainer has
`moveit_py` available. Then wire `MoveItPy` into `URMovementController`: add the
instance fields in `__init__`, declare the `moveit_connection_timeout` parameter,
start MoveItPy in a daemon thread inside `on_configure` (returning FAILURE on timeout
or exception), and tear it down in `on_cleanup`.

Purpose: All subsequent Phase 3 plans depend on `moveit_py` being importable and on
`self._moveit` / `self._planner_service` fields existing in the controller.

Output: Updated Dockerfile and `ur_movement_controller.py` with full MoveItPy lifecycle.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-moveit2-pilz-single-path-execution/03-CONTEXT.md
@.planning/phases/03-moveit2-pilz-single-path-execution/03-RESEARCH.md
@.planning/phases/03-moveit2-pilz-single-path-execution/03-PATTERNS.md
</context>

<interfaces>
<!-- Key patterns the executor must follow. -->

Existing ur_movement_controller.py __init__ (fields to extend):
  self._action_server: ActionServer | None = None
  self._is_active: bool = False
  self._is_executing: bool = False
  self._executing_lock: Lock = Lock()
  # ADD after _executing_lock:
  self._moveit: MoveItPy | None = None
  self._planner_service: PilzPlannerService | None = None

Existing on_configure structure (parameters block first, then action server creation):
  declare_parameter('action_server_name', ...)
  declare_parameter('moveit_group_name', ...)
  # ADD new parameter declaration here
  # THEN add MoveItPy init-with-timeout block AFTER parameter declarations
  # THEN create action server as today

on_cleanup structure (action server destroyed first, then new teardown):
  if self._action_server: destroy + set None
  # ADD: self._moveit = None; self._planner_service = None
  return TransitionCallbackReturn.SUCCESS

MoveItPy init-with-timeout pattern (Research §Pattern 5):
  - timeout = self.get_parameter('moveit_connection_timeout').value (float)
  - result_container: list = [None, None]  — [moveit_instance, exception]
  - define nested _init_moveit() that sets result_container[0] or [1]
  - daemon thread; init_thread.join(timeout=timeout)
  - if init_thread.is_alive(): log ERROR, return TransitionCallbackReturn.FAILURE
  - if result_container[1] is not None: log ERROR, return TransitionCallbackReturn.FAILURE
  - self._moveit = result_container[0]
  - moveit_group_name = self.get_parameter('moveit_group_name').value
  - planning_component = self._moveit.get_planning_component(moveit_group_name)
  - self._planner_service = PilzPlannerService(self._moveit, planning_component)

Imports to add at the top of ur_movement_controller.py (after existing imports):
  import threading
  from moveit.planning import MoveItPy
  from movement_controller.services.pilz_planner_service import PilzPlannerService
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ros-jazzy-moveit to Dockerfile apt block</name>
  <files>docker/Dockerfile</files>
  <read_first>
    - docker/Dockerfile — read the full file to see the existing apt-get install block structure
  </read_first>
  <action>
    In the existing apt-get install block (after ros-jazzy-nav2-lifecycle-manager and before sudo),
    add ros-jazzy-moveit as a new line. The apt-get update is already present in the block. Do NOT
    add a separate RUN layer — append to the existing multi-package install block so Docker layer
    caching is preserved. Keep the --no-install-recommends flag and the && rm -rf /var/lib/apt/lists/*
    cleanup at the end of the same block.
  </action>
  <verify>
    <automated>grep 'ros-jazzy-moveit' /workspaces/movement_controller/docker/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - docker/Dockerfile contains the string 'ros-jazzy-moveit' on a line inside the apt-get install block
    - The block still ends with '&& rm -rf /var/lib/apt/lists/*' (layer cleanup preserved)
    - There is exactly one RUN apt-get update block (no new separate RUN layer added for moveit)
  </acceptance_criteria>
  <done>Dockerfile updated; ros-jazzy-moveit present in apt block; grep confirms the line exists.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MoveItPy lifecycle into URMovementController</name>
  <files>src/movement_controller/movement_controller/ur_movement_controller.py</files>
  <read_first>
    - src/movement_controller/movement_controller/ur_movement_controller.py — read the full file;
      understand existing __init__, on_configure, on_cleanup structure before modifying
    - .planning/phases/03-moveit2-pilz-single-path-execution/03-CONTEXT.md — D-01 through D-05
    - .planning/phases/03-moveit2-pilz-single-path-execution/03-RESEARCH.md — Pattern 5 (MoveItPy connection with timeout), Pitfall 4 (no blocking kwarg)
    - .planning/phases/03-moveit2-pilz-single-path-execution/03-PATTERNS.md — controller section (imports, __init__, on_configure, on_cleanup patterns)
  </read_first>
  <action>
    Four changes to ur_movement_controller.py — make them in order:

    1. IMPORTS: Add 'import threading' after 'from threading import Lock'. Add
       'from moveit.planning import MoveItPy' in the external imports block. Add
       'from movement_controller.services.pilz_planner_service import PilzPlannerService'
       in the local imports block. BSD-3-Clause header is already present; do not modify it.

    2. __init__: After 'self._executing_lock: Lock = Lock()', add two new fields:
       'self._moveit: MoveItPy | None = None' and
       'self._planner_service: PilzPlannerService | None = None'.

    3. on_configure: After the two existing declare_parameter calls for action_server_name
       and moveit_group_name, declare the new parameter with name 'moveit_connection_timeout',
       default value 10.0 (float), and ParameterDescriptor description
       'Seconds to wait for MoveItPy to connect before failing on_configure'.
       Then, BEFORE action server creation (to fail fast if MoveItPy is unavailable),
       add the MoveItPy init-with-timeout block:
         - Read timeout from the declared parameter
         - Use a result_container list [None, None] with a nested _init_moveit daemon thread
         - _init_moveit() calls MoveItPy(node_name='moveit_py_node') inside try/except
         - init_thread.join(timeout=timeout)
         - If thread is alive: log 'MoveItPy failed to connect within {timeout}s timeout' at ERROR,
           return TransitionCallbackReturn.FAILURE
         - If result_container[1] is not None: log 'MoveItPy connection failed: {exception}' at ERROR,
           return TransitionCallbackReturn.FAILURE
         - Set self._moveit = result_container[0]
         - Get moveit_group_name from parameter and call self._moveit.get_planning_component(moveit_group_name)
         - Set self._planner_service = PilzPlannerService(self._moveit, planning_component)
         - Log info: 'MoveItPy connected; planning group: {moveit_group_name}'
       The action server creation block stays AFTER the MoveItPy init block.

    4. on_cleanup: After 'self._action_server = None', add:
       'self._moveit = None' and 'self._planner_service = None'.
       This mirrors the pattern for _action_server as specified in D-05.
  </action>
  <verify>
    <automated>grep -c 'moveit_connection_timeout' /workspaces/movement_controller/src/movement_controller/movement_controller/ur_movement_controller.py</automated>
  </verify>
  <acceptance_criteria>
    - Source contains 'self._moveit: MoveItPy | None = None' in __init__
    - Source contains 'self._planner_service: PilzPlannerService | None = None' in __init__
    - Source contains "declare_parameter('moveit_connection_timeout', 10.0," in on_configure
    - Source contains 'init_thread.join(timeout=timeout)' in on_configure
    - Source contains two occurrences of 'TransitionCallbackReturn.FAILURE' (added by this task; one for timeout, one for exception)
    - Source contains 'PilzPlannerService(self._moveit, planning_component)' in on_configure
    - Source contains 'self._moveit = None' inside on_cleanup body
    - Source contains 'self._planner_service = None' inside on_cleanup body
    - Source contains 'from moveit.planning import MoveItPy' in imports section
    - Source contains 'import threading' in imports section
  </acceptance_criteria>
  <done>
    on_configure connects MoveItPy with configurable timeout, returns FAILURE on connection error,
    and injects MoveItPy + planning_component into PilzPlannerService. on_cleanup destroys both references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dockerfile apt source → container | Packages installed from packages.ros.org/ros2/ubuntu (official ROS2 apt repo) |
| on_configure timeout thread | Thread runs in node's own process; no external input accepted during MoveItPy init |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Denial of Service | MoveItPy init thread | mitigate | Timeout via init_thread.join(timeout=) prevents indefinite blocking of on_configure |
| T-03-01-02 | Tampering | Dockerfile apt source | accept | ros-jazzy-moveit installed from official ROS2 apt repo (packages.ros.org/ros2/ubuntu); already in /etc/apt/sources.list.d/ros2.sources |
| T-03-01-SC | Tampering | apt installs | accept | ros-jazzy-moveit is an official ROS2 ecosystem package; legitimacy audit in RESEARCH.md confirms Approved disposition |
</threat_model>

<verification>
- `grep 'ros-jazzy-moveit' docker/Dockerfile` exits 0
- `grep 'moveit_connection_timeout' src/movement_controller/movement_controller/ur_movement_controller.py` exits 0
- `grep 'TransitionCallbackReturn.FAILURE' src/movement_controller/movement_controller/ur_movement_controller.py | wc -l` outputs ≥ 2
- `grep 'PilzPlannerService(self._moveit' src/movement_controller/movement_controller/ur_movement_controller.py` exits 0
</verification>

<success_criteria>
- Dockerfile contains ros-jazzy-moveit in the existing apt-get install block
- URMovementController has _moveit and _planner_service fields (both None at construction)
- on_configure declares moveit_connection_timeout float parameter with default 10.0
- on_configure spawns daemon thread for MoveItPy init; returns FAILURE on timeout or exception; logs ERROR in both cases
- on_cleanup sets _moveit and _planner_service to None
</success_criteria>

<output>
Create `.planning/phases/03-moveit2-pilz-single-path-execution/03-01-SUMMARY.md` when done.
</output>
