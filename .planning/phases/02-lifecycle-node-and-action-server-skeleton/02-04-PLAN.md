---
id: "02-04"
phase: 2
title: "Unit Tests — Lifecycle transitions, goal rejection, DTO validation, TrajectoryGrouper"
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - src/movement_controller/tests/unit/test_enums_and_dtos.py
  - src/movement_controller/tests/unit/test_trajectory_grouper.py
  - src/movement_controller/tests/unit/test_ur_movement_controller.py
  - src/movement_controller/CMakeLists.txt
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
  - ACT-04
  - ACT-05
must_haves:
  truths:
    - "test_enums_and_dtos.py tests MotionTypeEnum string values, FeedbackStatusEnum string values, TrajectoryPathDTO with invalid motion_type, empty path_id, negative blend_radius normalisation, and TrajectoryGoalDTO with empty paths"
    - "test_trajectory_grouper.py tests all D-07 grouping scenarios: single path, all-blend, no-blend, mixed (the 7-path D-07 example with 4 groups), and first-path-blend-ignored"
    - "test_ur_movement_controller.py tests goal rejection when node is not in ACTIVE state, goal rejection when _is_executing is True, and stub feedback sequence"
    - "CMakeLists.txt has exactly 3 new ament_add_pytest_test entries for test_enums_and_dtos, test_trajectory_grouper, and test_ur_movement_controller"
    - "colcon test passes all new test functions after colcon build --symlink-install"
  artifacts:
    - path: "src/movement_controller/tests/unit/test_enums_and_dtos.py"
      provides: "Unit tests for enums and DTOs"
      contains: "def test_"
    - path: "src/movement_controller/tests/unit/test_trajectory_grouper.py"
      provides: "Unit tests for TrajectoryGrouper"
      contains: "def test_"
    - path: "src/movement_controller/tests/unit/test_ur_movement_controller.py"
      provides: "Unit tests for URMovementController callbacks"
      contains: "def test_"
    - path: "src/movement_controller/CMakeLists.txt"
      provides: "Test registration for colcon test"
      contains: "ament_add_pytest_test(test_enums_and_dtos"
  key_links:
    - from: "test_ur_movement_controller.py"
      to: "URMovementController._goal_callback"
      via: "rclpy.init() fixture + patch.object on _state_machine.current_state"
      pattern: "patch.object.*_state_machine"
    - from: "CMakeLists.txt ament_add_pytest_test"
      to: "tests/unit/test_enums_and_dtos.py and others"
      via: "BUILD_TESTING block with APPEND_ENV PYTHONPATH"
      pattern: "ament_add_pytest_test.*test_enums_and_dtos"
---

<objective>
Create three unit test files and register them in CMakeLists.txt so `colcon test` discovers them. Tests cover the four D-16 targets: lifecycle rejection, goal rejection, DTO validation, and grouper algorithm.

Purpose: Provides automated regression coverage for Phase 2 behaviors before Phase 3 adds MoveIt2 planning. Ensures the stub feedback contract is verifiable without hardware.
Output: 3 test files, updated CMakeLists.txt.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-lifecycle-node-and-action-server-skeleton/02-CONTEXT.md
@.planning/phases/02-lifecycle-node-and-action-server-skeleton/02-RESEARCH.md
@.planning/phases/02-lifecycle-node-and-action-server-skeleton/02-02-SUMMARY.md
@.planning/phases/02-lifecycle-node-and-action-server-skeleton/02-03-SUMMARY.md
</context>

<interfaces>
<!-- Existing test pattern from Phase 1: src/movement_controller/tests/unit/test_imports.py -->
<!-- BSD-3-Clause header required on all new test files -->
<!-- pytest fixture for rclpy.init/shutdown: -->
<!--   @pytest.fixture(scope='function') -->
<!--   def ros_node(): -->
<!--       rclpy.init() -->
<!--       node = URMovementController() -->
<!--       yield node -->
<!--       node.destroy_node() -->
<!--       rclpy.shutdown() -->
<!-- Lifecycle state mock pattern: -->
<!--   from unittest.mock import patch -->
<!--   with patch.object(node, '_state_machine') as mock_sm: -->
<!--       mock_sm.current_state = (State.PRIMARY_STATE_INACTIVE, 'inactive') -->
<!--       result = node._goal_callback(mock_goal) -->
<!-- Async callback test pattern: -->
<!--   import asyncio -->
<!--   result = asyncio.run(node._execute_callback(mock_goal_handle)) -->
<!-- CMakeLists.txt ament_add_pytest_test pattern: see RESEARCH.md Pattern 8 -->
<!-- IMPORTANT: Each test file must be in a separate ament_add_pytest_test call -->
</interfaces>

<tasks>

<task id="02-04-T1" type="execute">
  <title>Create test_enums_and_dtos.py and test_trajectory_grouper.py</title>
  <read_first>
  - src/movement_controller/tests/unit/test_imports.py — BSD-3-Clause header + existing test structure; copy header verbatim for new test files
  - src/movement_controller/movement_controller/enums/motion_type_enum.py — values under test
  - src/movement_controller/movement_controller/enums/feedback_status_enum.py — values under test
  - src/movement_controller/movement_controller/models/trajectory_path_dto.py — all fields, validators, from_ros_msg
  - src/movement_controller/movement_controller/models/trajectory_goal_dto.py — paths validator
  - src/movement_controller/movement_controller/utils/trajectory_grouper.py — group() algorithm
  - .planning/phases/02-lifecycle-node-and-action-server-skeleton/02-CONTEXT.md — D-07 example (the 7-path example must be tested exactly), D-16 test targets
  </read_first>
  <action>
  **A. Create `src/movement_controller/tests/unit/test_enums_and_dtos.py`:**

  BSD-3-Clause header + module docstring: `"""Unit tests for MotionTypeEnum, FeedbackStatusEnum, TrajectoryPathDTO, and TrajectoryGoalDTO."""`

  Imports: `import pytest`, `from pydantic import ValidationError`, `from geometry_msgs.msg import PoseStamped, Point`, `from movement_controller.enums.motion_type_enum import MotionTypeEnum`, `from movement_controller.enums.feedback_status_enum import FeedbackStatusEnum`, `from movement_controller.models.trajectory_path_dto import TrajectoryPathDTO`, `from movement_controller.models.trajectory_goal_dto import TrajectoryGoalDTO`

  Helper at module scope: `def _make_path(**overrides) -> TrajectoryPathDTO` — builds a minimal valid `TrajectoryPathDTO` with defaults `path_id='p1'`, `motion_type=MotionTypeEnum.LIN`, `target_pose=PoseStamped()`, `circ_point=Point()`, then applies `overrides`. Use: `TrajectoryPathDTO(**{**defaults, **overrides})`.

  Test functions (each function name starts with `test_`):
  1. `test_motion_type_enum_values` — assert `MotionTypeEnum.LIN == 'LIN'`, `MotionTypeEnum.PTP == 'PTP'`, `MotionTypeEnum.CIRC == 'CIRC'`; assert `MotionTypeEnum.LIN` is an instance of `str`
  2. `test_feedback_status_enum_values` — assert `FeedbackStatusEnum.EXECUTING == 'executing'`, `FeedbackStatusEnum.COMPLETED == 'completed'`; assert `FeedbackStatusEnum.EXECUTING` is an instance of `str`
  3. `test_trajectory_path_dto_valid` — create a valid `TrajectoryPathDTO` with all required fields; assert `dto.path_id == 'test-uuid-1234'`, `dto.motion_type == MotionTypeEnum.LIN`, `dto.blend_radius == 0.0`
  4. `test_trajectory_path_dto_invalid_motion_type` — `pytest.raises(ValidationError)` when constructing with `motion_type='INVALID'`
  5. `test_trajectory_path_dto_empty_path_id` — `pytest.raises(ValidationError)` when constructing with `path_id=''`
  6. `test_trajectory_path_dto_negative_blend_radius_normalised` — construct with `blend_radius=-0.5`, assert `dto.blend_radius == 0.0` (no exception, silently normalised)
  7. `test_trajectory_path_dto_zero_blend_radius_unchanged` — construct with `blend_radius=0.0`, assert `dto.blend_radius == 0.0`
  8. `test_trajectory_path_dto_positive_blend_radius_unchanged` — construct with `blend_radius=0.3`, assert `dto.blend_radius == 0.3`
  9. `test_trajectory_path_dto_frozen` — construct valid DTO, then `pytest.raises` (either `ValidationError` or `TypeError`) when attempting `dto.path_id = 'new-id'`
  10. `test_trajectory_goal_dto_empty_paths` — `pytest.raises(ValidationError)` when constructing `TrajectoryGoalDTO(paths=[])`
  11. `test_trajectory_goal_dto_valid` — construct with one valid path; assert `len(dto.paths) == 1`

  **B. Create `src/movement_controller/tests/unit/test_trajectory_grouper.py`:**

  BSD-3-Clause header + module docstring: `"""Unit tests for TrajectoryGrouper blend grouping algorithm."""`

  Imports: `import pytest`, `from geometry_msgs.msg import PoseStamped, Point`, `from movement_controller.enums.motion_type_enum import MotionTypeEnum`, `from movement_controller.models.trajectory_path_dto import TrajectoryPathDTO`, `from movement_controller.utils.trajectory_grouper import TrajectoryGrouper`

  Helper: `def _p(path_id: str, blend_radius: float) -> TrajectoryPathDTO` — builds a minimal valid path with `motion_type=MotionTypeEnum.LIN`, `target_pose=PoseStamped()`, `circ_point=Point()`.

  Test functions:
  1. `test_empty_paths_raises` — `pytest.raises(ValueError, match='must not be empty')` when calling `TrajectoryGrouper.group([])`
  2. `test_duplicate_path_id_raises` — `pytest.raises(ValueError, match='Duplicate')` when calling with two paths sharing the same path_id
  3. `test_single_path_always_new_group` — input `[_p('a', 0.5)]` → assert `len(groups) == 1` and `len(groups[0]) == 1` (first path always starts a new group even if blend_radius > 0)
  4. `test_all_zero_blend_radius` — input `[_p('a', 0), _p('b', 0), _p('c', 0)]` → assert `len(groups) == 3`
  5. `test_mixed_grouping_d07_example` — the exact D-07 example: inputs `[('t0',0.5),('t1',0.0),('t2',0.0),('t3',0.3),('t4',0.3),('t5',0.3),('t6',0.0)]`; assert `len(groups) == 4`; assert `len(groups[0]) == 1` (t0); assert `len(groups[1]) == 1` (t1); assert `len(groups[2]) == 4` (t2,t3,t4,t5); assert `len(groups[3]) == 1` (t6)
  6. `test_all_positive_blend_radius_except_first` — input `[_p('a', 0.5), _p('b', 0.3), _p('c', 0.3)]` → assert `len(groups) == 1` (t0 starts group, t1 and t2 merge into it... wait, actually t0 always starts a NEW group. Then t1 (br=0.3 > 0 and i>0) merges into group[0]. Then t2 (br=0.3 > 0 and i>0) merges into group[0]). So 1 group with 3 members → assert `len(groups) == 1`, `len(groups[0]) == 3`
  7. `test_two_separate_groups` — input `[_p('a', 0.0), _p('b', 0.3), _p('c', 0.0), _p('d', 0.3)]` → groups: [[a], [b,c... wait no. a(br=0)→new, b(br=0.3, i>0)→merge into a's group, c(br=0.0)→new group, d(br=0.3, i>0)→merge into c's group → [[a,b], [c,d]] → 2 groups, each with 2 members]
  8. `test_negative_blend_radius_treated_as_zero` — input `[_p('a', -0.5), _p('b', 0.3)]` → dto normalises -0.5 to 0.0 so a has br=0.0. Groups: [[a], [b]] → 2 groups (a starts new group because... a is first path, always new group; b has br=0.3 > 0 and is not first → merges into a → [[a, b]] → actually this is 1 group! Let me reconsider. a is i=0 → always new group. b is i=1 with br=0.3 > 0 → merge into a's group. So [[a, b]] = 1 group. But after DTO normalisation, a.blend_radius=0.0, not -0.5. The grouper uses a.blend_radius for a's OWN check — but a is i=0 so it always starts a new group regardless. The important thing is b has br=0.3 > 0 → merges. So result is 1 group with paths [a, b].) Assert `len(groups) == 1` and `len(groups[0]) == 2`.
  </action>
  <verify>
    <automated>cd /workspaces/movement_controller && source /opt/ros/jazzy/setup.bash && source install/setup.bash && python3 -m pytest src/movement_controller/tests/unit/test_enums_and_dtos.py src/movement_controller/tests/unit/test_trajectory_grouper.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
  - `python3 -m pytest src/movement_controller/tests/unit/test_enums_and_dtos.py -v` exits with code 0; all test functions pass
  - `python3 -m pytest src/movement_controller/tests/unit/test_trajectory_grouper.py -v` exits with code 0; all test functions pass
  - `grep -c "^def test_" src/movement_controller/tests/unit/test_enums_and_dtos.py` returns 11
  - `grep -c "^def test_" src/movement_controller/tests/unit/test_trajectory_grouper.py` returns 8
  - `test_mixed_grouping_d07_example` passes: 7-path input produces 4 groups with sizes [1, 1, 4, 1]
  - BSD-3-Clause header in both files: `grep -c "Redistribution and use in source" src/movement_controller/tests/unit/test_enums_and_dtos.py` returns 1; same for test_trajectory_grouper.py
  </acceptance_criteria>
</task>

<task id="02-04-T2" type="execute">
  <title>Create test_ur_movement_controller.py and register all 3 new tests in CMakeLists.txt</title>
  <read_first>
  - src/movement_controller/tests/unit/test_imports.py — header pattern and simple test structure
  - src/movement_controller/CMakeLists.txt — current BUILD_TESTING block (lines 84–103); must add 3 ament_add_pytest_test entries after the existing test_imports entry, inside the same if(BUILD_TESTING) block, before the endif()
  - src/movement_controller/movement_controller/ur_movement_controller.py — current state; understand _goal_callback, _execute_callback, __init__ for mock planning
  - .planning/phases/02-lifecycle-node-and-action-server-skeleton/02-RESEARCH.md — Pattern 8 (CMakeLists.txt ament_add_pytest_test format, Pitfall 2)
  - .planning/phases/02-lifecycle-node-and-action-server-skeleton/02-CONTEXT.md — D-16 test targets (5 test areas)
  </read_first>
  <action>
  **A. Create `src/movement_controller/tests/unit/test_ur_movement_controller.py`:**

  BSD-3-Clause header + module docstring: `"""Unit tests for URMovementController lifecycle and action server callbacks."""`

  Imports: `import asyncio`, `import pytest`, `from unittest.mock import AsyncMock, MagicMock, patch`, `import rclpy`, `from lifecycle_msgs.msg import State`, `from rclpy.action import GoalResponse`, `from movement_controller.ur_movement_controller import URMovementController`

  **Fixtures (module-scoped to avoid repeated rclpy init/shutdown):**

  `@pytest.fixture(scope='module')` named `ros_context` — body: `rclpy.init()`, yield, then `rclpy.shutdown()`.

  `@pytest.fixture` named `node` (function-scoped) — depends on `ros_context` fixture; body: `n = URMovementController()`, yield `n`, `n.destroy_node()`.

  **Helper:** `def _make_ros_goal(path_id='uuid-1', motion_type='LIN')` — returns a `MagicMock` with `paths` as a list containing one mock path with `path_id=path_id` and `motion_type=motion_type`.

  **Test functions:**

  1. `test_goal_rejected_when_not_active(node)` — use `with patch.object(node, '_state_machine') as mock_sm:` and set `mock_sm.current_state = (State.PRIMARY_STATE_INACTIVE, 'inactive')`; call `result = node._goal_callback(_make_ros_goal())`; assert `result == GoalResponse.REJECT`

  2. `test_goal_rejected_when_unconfigured(node)` — same as above but `current_state = (State.PRIMARY_STATE_UNCONFIGURED, 'unconfigured')`; assert `GoalResponse.REJECT`

  3. `test_goal_rejected_when_executing(node)` — set `node._is_executing = True`; use `with patch.object(node, '_state_machine') as mock_sm:` and `mock_sm.current_state = (State.PRIMARY_STATE_ACTIVE, 'active')`; call `result = node._goal_callback(_make_ros_goal())`; assert `result == GoalResponse.REJECT`; clean up: `node._is_executing = False`

  4. `test_goal_rejected_empty_paths(node)` — patch state to ACTIVE; construct a goal mock where `goal.paths = []`; call `_goal_callback`; assert `GoalResponse.REJECT`

  5. `test_goal_rejected_empty_path_id(node)` — patch state to ACTIVE; set mock path `path_id=''`; assert `GoalResponse.REJECT`

  6. `test_goal_rejected_invalid_motion_type(node)` — patch state to ACTIVE; set mock path `motion_type='JUMP'`; assert `GoalResponse.REJECT`

  7. `test_goal_accepted_when_active_valid(node)` — patch state to ACTIVE; make sure `node._is_executing = False`; call `_goal_callback` with valid goal; assert `GoalResponse.ACCEPT`

  8. `test_execute_callback_stub_feedback_sequence(node)` — build a 2-path mock goal where path1 `(id='p1', br=0.0, motion_type='LIN')` and path2 `(id='p2', br=0.0, motion_type='LIN')` (each starts its own group → 2 groups → 4 feedback messages). Build `mock_goal_handle`:
     - `mock_goal_handle = MagicMock()`
     - `mock_goal_handle.request.paths` = list of two mock `TrajectoryPath` messages with appropriate fields set (`path_id`, `motion_type`, `blend_radius=0.0`, `target_pose=PoseStamped()`, `circ_point=Point()`, `cartesian_speed=0.0`, `acceleration=0.0`, `tool_frame=''`, `circ_type=''`)
     - `mock_goal_handle.publish_feedback = MagicMock()`
     - `mock_goal_handle.succeed = MagicMock()`
     - Call: `result = asyncio.run(node._execute_callback(mock_goal_handle))`
     - Assert `result.success is True`
     - Assert `mock_goal_handle.publish_feedback.call_count == 4` (2 groups × 2 feedback messages)
     - Assert `result.trajectory_paths_completed == ['p1', 'p2']`
     - Assert `mock_goal_handle.succeed.call_count == 1`

  9. `test_execute_callback_clears_is_executing_after_success(node)` — run the execute callback (as in test 8 but with one path), then assert `node._is_executing is False` after `asyncio.run(...)` completes

  Import `PoseStamped` and `Point` at the top of the test file: `from geometry_msgs.msg import PoseStamped, Point`

  **B. Update `src/movement_controller/CMakeLists.txt`:**

  Inside the `if(BUILD_TESTING)` block, AFTER the existing `ament_add_pytest_test(test_imports ...)` block and BEFORE `endif()`, add three new `ament_add_pytest_test` calls following the exact same format as the existing `test_imports` entry (with APPEND_ENV, ENV PYTEST_ADDOPTS, TIMEOUT 60, WORKING_DIRECTORY):

  - `ament_add_pytest_test(test_enums_and_dtos "tests/unit/test_enums_and_dtos.py" ...)` with the same APPEND_ENV/ENV/TIMEOUT/WORKING_DIRECTORY arguments
  - `ament_add_pytest_test(test_trajectory_grouper "tests/unit/test_trajectory_grouper.py" ...)` same
  - `ament_add_pytest_test(test_ur_movement_controller "tests/unit/test_ur_movement_controller.py" ...)` same

  After this change, run `colcon build --symlink-install` (rebuilds CMakeLists.txt targets) before `colcon test`.
  </action>
  <verify>
    <automated>cd /workspaces/movement_controller && source /opt/ros/jazzy/setup.bash && source install/setup.bash && python3 -m pytest src/movement_controller/tests/unit/test_ur_movement_controller.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
  - `python3 -m pytest src/movement_controller/tests/unit/test_ur_movement_controller.py -v` exits with code 0; all 9 test functions pass
  - `grep -c "^def test_" src/movement_controller/tests/unit/test_ur_movement_controller.py` returns 9
  - `grep "ament_add_pytest_test(test_enums_and_dtos" src/movement_controller/CMakeLists.txt` returns a match
  - `grep "ament_add_pytest_test(test_trajectory_grouper" src/movement_controller/CMakeLists.txt` returns a match
  - `grep "ament_add_pytest_test(test_ur_movement_controller" src/movement_controller/CMakeLists.txt` returns a match
  - All 3 new `ament_add_pytest_test` entries are inside the `if(BUILD_TESTING)` block (verified by visual inspection or: `grep -c "ament_add_pytest_test" src/movement_controller/CMakeLists.txt` returns 4)
  - `colcon build --symlink-install --packages-select movement_controller` succeeds after CMakeLists.txt changes
  - `colcon test --packages-select movement_controller && colcon test-result --verbose` shows test_enums_and_dtos, test_trajectory_grouper, and test_ur_movement_controller listed alongside test_imports with all passing
  - BSD-3-Clause header in test file: `grep -c "Redistribution and use in source" src/movement_controller/tests/unit/test_ur_movement_controller.py` returns 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test inputs → production code under test | Synthetic inputs cross into validators and callbacks; no external network involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | test fixture rclpy.shutdown scope | mitigate | Module-scoped `ros_context` fixture ensures single init/shutdown pair per test module; function-scoped `node` fixture ensures clean node per test |
| T-02-17 | DoS | asyncio.run in test | accept | `asyncio.run()` used for testing async callback in sync pytest; no infinite loops in stub execute_callback (no sleep, no blocking I/O) |
| T-02-18 | Information Disclosure | test assertions expose internal state (_is_executing, _state_machine) | accept | Tests are in the test package, not shipped to production; internal state access is legitimate for unit testing |
| T-02-SC | Tampering | pip/apt installs | accept | No new packages; pytest already declared in package.xml test_depend from Phase 1 |
</threat_model>

<verification>
After completing both tasks:

1. `python3 -m pytest src/movement_controller/tests/unit/ -v` exits with code 0; all 4 test files pass (test_imports + 3 new)
2. `grep -c "ament_add_pytest_test" src/movement_controller/CMakeLists.txt` returns 4
3. `colcon build --symlink-install --packages-select movement_controller` succeeds
4. `colcon test --packages-select movement_controller` succeeds
5. `colcon test-result --verbose` lists all 4 test files with PASSED status
6. `test_execute_callback_stub_feedback_sequence` asserts 4 feedback messages for a 2-group goal (2 executing + 2 completed) and `result.success is True`
</verification>

<success_criteria>
- 9 test functions in test_ur_movement_controller.py cover: goal rejection (not-ACTIVE state, not-UNCONFIGURED state, _is_executing=True, empty paths, empty path_id, invalid motion_type), goal acceptance when valid, full feedback sequence (4 messages for 2-group goal), _is_executing cleared after completion
- 11 test functions in test_enums_and_dtos.py cover: enum string values, DTO validation (invalid motion_type, empty path_id, negative blend_radius normalisation, frozen model), TrajectoryGoalDTO empty paths rejection
- 8 test functions in test_trajectory_grouper.py cover: all D-07 grouping scenarios including the exact 7-path example producing 4 groups
- CMakeLists.txt has 4 total ament_add_pytest_test entries (test_imports + 3 new)
- `colcon test` discovers and passes all 4 test files after `colcon build --symlink-install`
- BSD-3-Clause header in all 3 new test files
</success_criteria>

<output>
Create `.planning/phases/02-lifecycle-node-and-action-server-skeleton/02-04-SUMMARY.md` when done.
</output>
