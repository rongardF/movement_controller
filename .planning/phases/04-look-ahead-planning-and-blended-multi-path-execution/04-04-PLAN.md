---
plan_id: 04-04
phase: 4
title: Tests — Unit and Integration for Look-Ahead and Blended Execution
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - src/movement_controller/tests/unit/test_pilz_planner_service.py
  - src/movement_controller/tests/integration/test_moveit_execution_integration.py
autonomous: true
requirements: [MOT-02, MOT-03, MOT-04]
status: pending

must_haves:
  truths:
    - "New unit tests verify plan_all starts a thread, iterate yields PlanResultDTOs, cancel terminates the iterator"
    - "New unit test verifies last item in group always has blend_radius=0.0 (PILZ constraint)"
    - "Integration test for 3-path trajectory verifies 2 feedback pairs (group-level, per D-01)"
    - "Integration test verifies completed_ids contains all 3 path IDs in execution order (D-02)"
    - "Integration test for cancel verifies goal_handle.canceled() is called and planner.cancel() is called"
    - "Existing integration tests are updated to work with new API (plan_all + iterate, not plan())"
    - "All existing unit tests for plan(), _build_circ_constraints still pass"
    - "Look-ahead timing test: group 1 is pre-planned before group 0 execute_and_wait returns (temporal overlap assertion)"
  artifacts:
    - path: "src/movement_controller/tests/unit/test_pilz_planner_service.py"
      provides: "Phase 4 unit tests for plan_all/iterate/cancel and last-blend-radius constraint"
      contains: "test_plan_all_starts_background_thread"
    - path: "src/movement_controller/tests/integration/test_moveit_execution_integration.py"
      provides: "Updated fixtures + new integration tests for 3-path and cancel scenarios"
      contains: "test_execute_3_path_blended_trajectory_success"
      also_contains: "test_look_ahead_plans_next_group_before_current_finishes"
  key_links:
    - from: "test_pilz_planner_service.py"
      to: "PilzPlannerService.plan_all"
      via: "mock service client returning pre-built response"
      pattern: "plan_all"
    - from: "test_moveit_execution_integration.py"
      to: "URMovementController._execute_callback"
      via: "asyncio.run(_execute_callback(mock_goal_handle))"
      pattern: "iterate_planned_trajectories"
---

<objective>
Write tests that prove the Phase 4 feature works end-to-end under simulation conditions:
- Unit tests mock the /plan_sequence_path service client to verify look-ahead logic in isolation
- Integration tests mock the entire planner service and TEM to verify _execute_callback orchestration
- Existing integration tests are updated to use the new plan_all/iterate API instead of the
  removed plan() call, preventing false-positive regressions from stale test assumptions

Purpose: MOT-02/03/04 acceptance validation without hardware.
Output: Extended unit test file and updated integration test file.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-look-ahead-planning-and-blended-multi-path-execution/04-CONTEXT.md
@src/movement_controller/tests/unit/test_pilz_planner_service.py
@src/movement_controller/tests/integration/test_moveit_execution_integration.py
@src/movement_controller/movement_controller/services/pilz_planner_service.py
@src/movement_controller/movement_controller/models/plan_result_dto.py

<interfaces>
<!-- Phase 4 PilzPlannerService API (from Plans 04-01/02) -->
PilzPlannerService.__init__(moveit, moveit_group_name, node)
PilzPlannerService.plan_all(groups: list[list[TrajectoryPathDTO]]) -> None
PilzPlannerService.iterate_planned_trajectories() -> Iterator[PlanResultDTO]
PilzPlannerService.cancel() -> None
PilzPlannerService.wait_for_service(timeout_sec) -> bool

<!-- PlanResultDTO fields (from Plan 04-01) -->
success: bool
trajectory: Any = None
trajectories: list[Any] = []
path_ids: list[str] = []
blended: bool = False
error_message: str = ''

<!-- Phase 4 _execute_callback API expectations (from Plan 04-03) -->
# Calls planner_service.plan_all(groups)
# Iterates planner_service.iterate_planned_trajectories()
# Publishes fb.trajectory_path_ids = plan_dto.path_ids  (list, not single str)
# Calls tem.push(RobotTrajectory(...)) + tem.execute_and_wait() per group
# On success: result.trajectory_paths_completed = flat list of all path_ids

<!-- Mocking the service client response for unit tests -->
# Build GetMotionSequence.Response:
# resp = MagicMock()
# resp.response.error_code.val = 1  # MoveItErrorCodes.SUCCESS
# resp.response.planned_trajectories = [mock_ros_traj1, ...]
# future = MagicMock(); future.done.return_value = True; future.result.return_value = resp
# node.create_client.return_value.call_async.return_value = future

<!-- BSD-3-Clause header must be present at top of each test file (already present in both) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New unit tests for plan_all, iterate, cancel, and blend_radius constraint</name>
  <files>src/movement_controller/tests/unit/test_pilz_planner_service.py</files>
  <read_first>
    - src/movement_controller/tests/unit/test_pilz_planner_service.py (full file — understand existing fixtures, _make_path_dto helper, test structure)
    - src/movement_controller/movement_controller/services/pilz_planner_service.py (plan_all, iterate_planned_trajectories, cancel, _plan_group_sequence signatures)
  </read_first>
  <behavior>
    - test_plan_all_starts_background_thread: after plan_all([[[path_dto]]], svc._planning_thread.is_alive() is True (before iterator is drained)
    - test_iterate_yields_single_path_result: single-path group → iterate yields one PlanResultDTO with success=True, path_ids=[_UUID], blended=False, len(trajectories)==1
    - test_iterate_blended_group_sets_blended_true: two-path group → yields PlanResultDTO with blended=True, len(path_ids)==2
    - test_last_item_blend_radius_forced_to_zero: group with two paths (first blend_radius=0.05, second blend_radius=0.05) → verify service client called with items[-1].blend_radius == 0.0
    - test_cancel_terminates_iterator_cleanly: plan_all([[path1, path2, path3]]) with service blocking; cancel() → list(iterate_planned_trajectories()) returns quickly without hanging (timeout 2s)
    - test_planning_failure_yields_error_dto: service client returns error_code.val != 1 → iterate yields PlanResultDTO(success=False) and terminates
    - test_plan_all_creates_fresh_queue_per_call: call plan_all twice; assert second call's queue is a different object than first call's queue (D-04)
  </behavior>
  <action>
    ADD new fixtures at the bottom of the existing fixtures section (after mock_node):

    Fixture mock_seq_response: builds a mock GetMotionSequence response:
    - Creates a mock RobotTrajectory with joint_trajectory.joint_names=['j1'] and
      joint_trajectory.points=[MagicMock(positions=[0.0])]
    - Sets response.response.error_code.val = 1 (SUCCESS)
    - Sets response.response.planned_trajectories = [the mock trajectory]
    - Returns the response mock

    Fixture mock_seq_future(mock_seq_response): future where done()=True, result()=mock_seq_response

    Fixture mock_node_with_seq(mock_seq_future): node mock where
    node.create_client.return_value.call_async.return_value = mock_seq_future

    Fixture service_with_seq(mock_node_with_seq): builds PilzPlannerService with:
    - A mock moveit that has get_planning_component, get_planning_scene_monitor
    - The get_planning_scene_monitor().read_only().__enter__().current_state = MagicMock()
    - Patches moveit.core.robot_state.robotStateToRobotStateMsg to return a MagicMock
    - group_name = 'ur_manipulator'

    ADD test section "# region: Phase 4 — plan_all / iterate / cancel" with tests:

    test_plan_all_starts_background_thread(service_with_seq):
    - Create one-path group using _make_path_dto()
    - Call service_with_seq.plan_all([[path_dto]])
    - Wait briefly (0.05s) then assert service_with_seq._planning_thread.is_alive()
    - Drain iterator to let thread finish

    test_iterate_yields_single_path_result(service_with_seq):
    - Call plan_all([[_make_path_dto()]])
    - results = list(iterate_planned_trajectories())  ← drains generator
    - Assert len(results) == 1
    - Assert results[0].success is True
    - Assert results[0].path_ids == [_UUID]
    - Assert results[0].blended is False
    - Assert len(results[0].trajectories) == 1

    test_iterate_blended_group_sets_blended_true(service_with_seq, mock_seq_response):
    - Configure mock response to have 2 planned_trajectories (extend the list)
    - Two paths: _UUID and _UUID2 = 'bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb'
    - Call plan_all([[path1, path2]])
    - result = next(iterate_planned_trajectories())
    - Assert result.blended is True
    - Assert result.path_ids == [_UUID, _UUID2]

    test_last_item_blend_radius_forced_to_zero(service_with_seq):
    - Two paths: path1(blend_radius=0.05), path2(blend_radius=0.05)
    - Call plan_all([[path1, path2]])
    - Drain iterator
    - Inspect call_async call: get the MotionSequenceRequest from call_async.call_args
    - Assert the items[-1].blend_radius == 0.0
    Note: the call_async receives a GetMotionSequence.Request; access
    call_args[0][0].request.items[-1].blend_radius
    If MotionSequenceItem is a real msg object, assert it directly. If mocked, assert the
    mock was constructed correctly. Simplest: verify by running real msg construction and
    inspecting the built items before call_async is invoked (use a side_effect on call_async
    to capture the request).

    test_cancel_terminates_iterator_cleanly(service_with_seq, mock_node_with_seq):
    - Configure the future to never complete: future.done.return_value = False
    - Call plan_all([[_make_path_dto(), _make_path_dto(path_id=_UUID2)]])
    - In a separate thread after 0.05s, call service_with_seq.cancel()
    - Assert list(iterate_planned_trajectories()) returns within 2 seconds
    - Use threading.Timer or a Thread with join(timeout=2) to verify no hang

    test_planning_failure_yields_error_dto(service_with_seq, mock_seq_future):
    - Override future.result.return_value.response.error_code.val = 99  (not SUCCESS)
    - Call plan_all([[_make_path_dto()]])
    - results = list(iterate_planned_trajectories())
    - Assert len(results) == 1
    - Assert results[0].success is False
    - Assert results[0].error_message != ''

    test_plan_all_creates_fresh_queue_per_call(service_with_seq):
    - Call plan_all([[_make_path_dto()]]); drain iterator
    - q1 = service_with_seq._plan_queue
    - Call plan_all([[_make_path_dto()]]); drain iterator
    - q2 = service_with_seq._plan_queue
    - Assert q1 is not q2 (D-04: fresh queue per call)
  </action>
  <verify>
    <automated>cd /workspaces/movement_controller && source /opt/ros/jazzy/setup.bash && source install/setup.bash && python -m pytest src/movement_controller/tests/unit/test_pilz_planner_service.py -v -k "phase4 or plan_all or iterate or cancel or blend_radius or planning_failure or fresh_queue" 2>&1 | tail -30</automated>
  </verify>
  <done>
    All 7 new Phase 4 unit tests pass.
    All existing Phase 3 unit tests still pass (no regressions).
    python -m pytest src/movement_controller/tests/unit/test_pilz_planner_service.py -v → all PASSED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update integration tests for Phase 4 API + new multi-path and cancel tests</name>
  <files>src/movement_controller/tests/integration/test_moveit_execution_integration.py</files>
  <read_first>
    - src/movement_controller/tests/integration/test_moveit_execution_integration.py (full file — understand current node_with_moveit fixture and all existing tests)
    - src/movement_controller/movement_controller/ur_movement_controller.py (_execute_callback after Plan 04-03 — understand what the mock must satisfy)
  </read_first>
  <behavior>
    Existing tests after update:
    - test_execute_trajectory_single_path_success: result.success=True, completed=['id1'], 2 feedback calls
    - test_execute_trajectory_feedback_order: first feedback.status='executing', second='completed'; trajectory_path_ids is a list
    - test_execute_trajectory_aborts_on_plan_failure: iterate returns PlanResultDTO(success=False) → result.success=False, abort called
    - test_execute_trajectory_aborts_on_execution_failure: execute_and_wait raises → result.success=False, abort called

    New tests:
    - test_execute_3_path_blended_trajectory_success: 3 paths in 2 groups → 4 feedback calls, completed=['id1','id2','id3']
    - test_cancel_during_execution_returns_canceled: goal_handle.is_cancel_requested=True → canceled() called, cancel() called on planner, partial completed_ids
    - test_look_ahead_plans_next_group_before_current_finishes: group 0 result is already queued in _plan_queue before consumer iterates (temporal overlap — satisfies MOT-03 success criterion)
  </behavior>
  <action>
    === UPDATE node_with_moveit FIXTURE ===
    The existing fixture injects mock_planner with a plan() method. Replace the planner mock setup:

    Remove:
      mock_planner.plan.return_value = MagicMock(success=True, trajectory=MagicMock(), error_message='')

    Add to mock_planner:
      mock_planner.plan_all = MagicMock()
      mock_planner.cancel = MagicMock()
      # Default iterate: yields one success PlanResultDTO for _UUID1, then terminates
      # (override per-test using monkeypatch or direct attribute assignment)

    Add to mock_moveit:
      # TEM mock
      mock_tem = MagicMock()
      mock_tem.execute_and_wait = MagicMock(return_value=None)
      mock_moveit.get_trajectory_execution_manager.return_value = mock_tem
      # robot_model mock
      mock_moveit.get_robot_model.return_value = MagicMock()
      # scene monitor for ref_state (already partially mocked; ensure read_only() context works)
      mock_scene_ctx = MagicMock()
      mock_scene_ctx.__enter__ = MagicMock(return_value=MagicMock(current_state=MagicMock()))
      mock_scene_ctx.__exit__ = MagicMock(return_value=False)
      mock_moveit.get_planning_scene_monitor.return_value.read_only.return_value = mock_scene_ctx

    Add helper fixture _make_plan_dto(path_ids, blended=False):
      Creates PlanResultDTO(success=True, path_ids=path_ids, blended=blended,
                            trajectories=[MagicMock() for _ in path_ids])

    Patch `movement_controller.ur_movement_controller.RobotTrajectory` in the fixture
    so it does not require real moveit.core.robot_trajectory:
      with patch('movement_controller.ur_movement_controller.RobotTrajectory') as mock_rt:
          mock_rt.return_value = MagicMock()
          # yield node inside this patch context

    === UPDATE EXISTING TESTS ===

    For each existing test, replace mock_planner.plan call setup with iterate_planned_trajectories:

    test_execute_trajectory_single_path_success:
    - Remove: monkeypatch of plan() return value (if any)
    - Add: node_with_moveit._planner_service.iterate_planned_trajectories.return_value = iter([
          PlanResultDTO(success=True, path_ids=[_UUID1], blended=False, trajectories=[MagicMock()])
      ])
    - Assert: result.success is True, result.trajectory_paths_completed == [_UUID1]
    - Assert: 2 feedback calls (executing + completed)

    test_execute_trajectory_feedback_order:
    - Set iterate to yield one dto with path_ids=[_UUID1]
    - Assert calls[0].args[0].trajectory_path_ids == [_UUID1]  (list, not bare str)
    - Assert calls[0].args[0].status == 'executing'
    - Assert calls[1].args[0].status == 'completed'

    test_execute_trajectory_aborts_on_plan_failure:
    - Set iterate to yield PlanResultDTO(success=False, path_ids=[_UUID1], error_message='planning failed')
    - Assert result.success is False
    - Assert goal_handle.abort.assert_called_once()

    test_execute_trajectory_aborts_on_execution_failure:
    - Set iterate to yield success dto
    - Set mock_tem.execute_and_wait.side_effect = Exception('TEM error')
    - Assert result.success is False
    - Assert goal_handle.abort.assert_called_once()

    test_execute_trajectory_circ_path_success (if exists):
    - Update similarly to single-path success pattern

    === ADD NEW TESTS ===

    test_execute_3_path_blended_trajectory_success(node_with_moveit):
    - 3 path IDs: _UUID1, _UUID2='...2', _UUID3='...3'
    - Set iterate to yield 2 DTOs:
        dto1: path_ids=[_UUID1, _UUID2], blended=True, trajectories=[MagicMock(), MagicMock()]
        dto2: path_ids=[_UUID3], blended=False, trajectories=[MagicMock()]
      (use iter([dto1, dto2]) as return_value of iterate_planned_trajectories)
    - goal_handle.request.paths = [_make_path_msg(_UUID1), _make_path_msg(_UUID2), _make_path_msg(_UUID3)]
    - result = asyncio.run(node._execute_callback(mock_goal_handle))
    - Assert result.success is True
    - Assert result.trajectory_paths_completed == [_UUID1, _UUID2, _UUID3]
    - Assert mock_goal_handle.publish_feedback.call_count == 4  (2 executing + 2 completed)
    - Assert node._planner_service.plan_all.call_count == 1 (called exactly once)

    test_cancel_during_execution_returns_canceled(node_with_moveit):
    - Set goal_handle.is_cancel_requested = True (cancel detected immediately at start of first group)
    - Set iterate to yield one dto with path_ids=[_UUID1]  (would execute if not canceled)
    - result = asyncio.run(node._execute_callback(mock_goal_handle))
    - Assert result.success is False
    - Assert result.error_message contains 'canceled' (case-insensitive)
    - Assert mock_goal_handle.canceled.assert_called_once()
    - Assert node._planner_service.cancel.assert_called()  (called by _execute_callback on cancel path)

    test_look_ahead_plans_next_group_before_current_finishes(service_with_seq):
    PURPOSE: Satisfy ROADMAP MOT-03 success criterion — group N+1 planned before group N execute_and_wait returns.
    - Set up PilzPlannerService with 2 groups, each single-path: [[path1], [path2]]
    - Wrap mock_seq_future.done so the FIRST call returns False once (simulates in-progress planning),
      then True thereafter. This makes the background thread loop once before completing group 0.
    - Call plan_all([[path1], [path2]])
    - In a side_effect closure on mock_tem.execute_and_wait for the first call:
        Check service_with_seq._plan_queue.qsize() >= 1 (group 1 result already queued)
        Record queue_size_during_execute = service_with_seq._plan_queue.qsize()
    - Wait for the planning thread to finish (join with timeout=3s)
    - Drain iterator (list(iterate_planned_trajectories()))
    - Assert queue_size_during_execute >= 1
    NOTE: This is a UNIT test in test_pilz_planner_service.py (not the integration file).
    The side_effect on execute_and_wait is NOT available in PilzPlannerService — the look-ahead
    test instead checks _plan_queue state: after plan_all([[path1], [path2]]) with a
    blocking future (done=False for 50ms), assert that after group 0's future resolves,
    _plan_queue has at least one item before the caller (controller) begins iterating.
    SIMPLIFIED IMPLEMENTATION: Use threading.Event to synchronize:
      - event_group0_queued = threading.Event()
      - Patch _plan_group_sequence to set event_group0_queued after first group result is
        put into the queue, then sleep 0.1s before planning group 1
      - Call plan_all([[path1], [path2]])
      - event_group0_queued.wait(timeout=2)
      - Assert _plan_queue.qsize() >= 1 BEFORE calling list(iterate_planned_trajectories())
      - This proves group 0 was queued (look-ahead occurred) before consumer started draining
  </action>
  <verify>
    <automated>cd /workspaces/movement_controller && source /opt/ros/jazzy/setup.bash && source install/setup.bash && python -m pytest src/movement_controller/tests/integration/test_moveit_execution_integration.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    All integration tests pass (both updated existing tests and new tests).
    python -m pytest src/movement_controller/tests/ -v → all PASSED (unit + integration).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → mocked external services | All external boundaries (ROS2 service, TEM, hardware) are mocked; no real network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | Test output showing path IDs (UUID4) | accept | Test IDs are hardcoded constants with no PII |
| T-04-04-SC | Tampering | No new packages installed | accept | pytest and MagicMock are existing test dependencies |
</threat_model>

<verification>
Full test suite (all unit + integration tests):

```bash
cd /workspaces/movement_controller
source /opt/ros/jazzy/setup.bash && source install/setup.bash
python -m pytest src/movement_controller/tests/ -v 2>&1 | tail -40
```

All tests must pass. Check counts:
- Unit tests: existing ≥9 + new Phase 4 ≥7 = ≥16
- Integration tests: existing (updated) ≥5 + new ≥2 = ≥7

```bash
grep -c 'def test_' src/movement_controller/tests/unit/test_pilz_planner_service.py
grep -c 'def test_' src/movement_controller/tests/integration/test_moveit_execution_integration.py
```
</verification>

<success_criteria>
- python -m pytest src/movement_controller/tests/ → all PASSED, zero FAILED
- test_execute_3_path_blended_trajectory_success passes: 4 feedback calls, trajectory_paths_completed=['id1','id2','id3']
- test_cancel_during_execution_returns_canceled passes: goal_handle.canceled() called
- test_plan_all_starts_background_thread passes: thread is alive after plan_all()
- test_last_item_blend_radius_forced_to_zero passes: items[-1].blend_radius == 0.0 asserted
- No existing Phase 3 tests regressed
</success_criteria>

<output>
Create .planning/phases/04-look-ahead-planning-and-blended-multi-path-execution/04-04-SUMMARY.md when done
</output>
