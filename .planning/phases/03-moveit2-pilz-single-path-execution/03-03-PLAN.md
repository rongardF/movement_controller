---
phase: "03-moveit2-pilz-single-path-execution"
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/movement_controller/movement_controller/models/trajectory_path_dto.py
  - src/movement_controller/movement_controller/ur_movement_controller.py
  - src/movement_controller/tests/unit/test_enums_and_dtos.py
autonomous: true
requirements:
  - MOT-01
  - MOT-02

must_haves:
  truths:
    - "TrajectoryPathDTO.from_ros_msg raises ValueError if motion_type==CIRC and circ_type is empty or not in {'interim','center'}"
    - "Empty circ_type for CIRC paths is never silently mapped to INTERIM; it always raises ValueError with path_id in the message"
    - "_goal_callback's existing except (ValidationError, ValueError) block catches this ValueError and returns GoalResponse.REJECT — no new code added to _goal_callback for CIRC validation"
    - "_execute_callback calls TrajectoryGrouper.group() then flattens all groups to individual paths (D-13)"
    - "For each flattened path: executing feedback published first, then PilzPlannerService.plan() called, then execute, then completed feedback"
    - "If plan() returns success=False: log ERROR, call goal_handle.abort(), return Result(success=False, error_message=plan_result.error_message)"
    - "If moveit.execute() returns falsy: log ERROR, call goal_handle.abort(), return Result(success=False, error_message identifying path_id)"
    - "moveit.execute() is called as self._moveit.execute(plan_result.trajectory, controllers=[]) — no blocking kwarg"
    - "On success: goal_handle.succeed() called, Result(success=True, trajectory_paths_completed=[all_path_ids])"
    - "5 new from_ros_msg tests added to test_enums_and_dtos.py: empty CIRC raises ValueError, invalid CIRC raises ValueError, valid interim succeeds, valid center succeeds, non-CIRC empty still defaults to INTERIM"
    - "All existing test_enums_and_dtos.py tests still pass after the from_ros_msg change"
  artifacts:
    - path: "src/movement_controller/tests/unit/test_enums_and_dtos.py"
      provides: "Updated unit tests for from_ros_msg CIRC validation"
      contains: "test_from_ros_msg_circ_empty_circ_type_raises"
  key_links:
    - from: "TrajectoryPathDTO.from_ros_msg"
      to: "GoalResponse.REJECT"
      via: "raises ValueError for CIRC with empty/invalid circ_type; caught by existing _goal_callback except block"
      pattern: "raise ValueError.*circ_type"
    - from: "_execute_callback"
      to: "PilzPlannerService.plan"
      via: "self._planner_service.plan(path)"
      pattern: "self\\._planner_service\\.plan\\("
    - from: "_execute_callback"
      to: "MoveItPy.execute"
      via: "self._moveit.execute(plan_result.trajectory, controllers=[])"
      pattern: "self\\._moveit\\.execute\\("
---

<objective>
Replace the Phase 2 stub in `_execute_callback` with the real plan-then-execute loop,
and harden CIRC validation inside `TrajectoryPathDTO.from_ros_msg`. Together these satisfy
MOT-01 (PILZ is the planner driving each path) and MOT-02 (single-path execution foundation
for eventual blended multi-path).

Purpose: After this plan the controller plans each path via PILZ and executes it immediately,
sending per-path `executing` → `completed` feedback. Phase 4 will replace the single-path
loop with `MoveGroupSequence` blending.

Output: `ur_movement_controller.py` with real planning and execution in `_execute_callback`,
plus `trajectory_path_dto.py` with CIRC validation that raises ValueError caught by the
existing `_goal_callback` except block — no new code added to `_goal_callback`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-moveit2-pilz-single-path-execution/03-CONTEXT.md
@.planning/phases/03-moveit2-pilz-single-path-execution/03-RESEARCH.md
@.planning/phases/03-moveit2-pilz-single-path-execution/03-PATTERNS.md
@.planning/phases/02-lifecycle-node-and-action-server-skeleton/02-CONTEXT.md
</context>

<interfaces>
<!-- Current state of ur_movement_controller.py that this plan modifies. -->

Current _goal_callback structure (UNCHANGED in Phase 3):
  1. _is_active check → REJECT
  2. _is_executing lock acquire → REJECT if already executing
  3. TrajectoryGoalDTO.from_ros_msg(goal) in try/except → REJECT on ValidationError/ValueError
  4. return GoalResponse.ACCEPT
  NO new step is added to _goal_callback. CIRC validation is performed inside from_ros_msg
  (Task 1), which raises ValueError — already caught by the existing step 3 except block.

TrajectoryPathDTO.from_ros_msg circ_type logic (current → new):
  CURRENT (to be removed):
    if ros_msg.circ_type == '' or ros_msg.circ_type is None:
        circ_type = CircTypeEnum.INTERIM   # ← silent default; masks invalid CIRC paths
    else:
        circ_type = CircTypeEnum(ros_msg.circ_type)
  NEW:
    if motion_type == MotionTypeEnum.CIRC:
        if not ros_msg.circ_type:   # empty string or None
            raise ValueError(
                f'path {ros_msg.path_id!r} has motion_type CIRC but circ_type is empty; '
                f'must be "interim" or "center"'
            )
        circ_type = CircTypeEnum(ros_msg.circ_type)   # raises ValueError if unrecognized
    else:
        circ_type = CircTypeEnum(ros_msg.circ_type) if ros_msg.circ_type else CircTypeEnum.INTERIM

Current _execute_callback structure (to be replaced):
  - Calls from_ros_msg, groups paths, sends stub feedback per GROUP
  Phase 3 replaces the inner loop with path-level flatten + plan + execute:
    for group in groups:
      for path in group:
        publish feedback(executing, [path.path_id])
        plan_result = self._planner_service.plan(path)
        if not plan_result.success:
          log ERROR; abort; return Result(success=False, ...)
        exec_status = self._moveit.execute(plan_result.trajectory, controllers=[])
        if not exec_status:
          log ERROR with path.path_id; abort; return Result(success=False, ...)
        publish feedback(completed, [path.path_id])
        completed_ids.append(path.path_id)
    succeed; return Result(success=True, trajectory_paths_completed=completed_ids)

  CRITICAL: execute() signature is moveit.execute(trajectory, controllers=[])
  — NO blocking kwarg (Research §Pitfall 4).
  NOTE: D-09 specified 'blocking=True' in the execute() call. Research Pitfall 4
  confirms this kwarg does not exist in the MoveItPy.execute() C++ binding and would
  raise TypeError at runtime. This plan removes blocking=True, correcting D-09.

  The outer except/finally pattern is unchanged from Phase 2:
    except Exception: log ERROR; abort; return failure result
    finally: release _is_executing flag
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove silent circ_type default in TrajectoryPathDTO.from_ros_msg</name>
  <files>src/movement_controller/movement_controller/models/trajectory_path_dto.py</files>
  <read_first>
    - src/movement_controller/movement_controller/models/trajectory_path_dto.py — read the full
      from_ros_msg classmethod; understand the current circ_type mapping block (lines ~101–130)
    - src/movement_controller/movement_controller/enums/motion_type_enum.py — confirm MotionTypeEnum.CIRC value
    - src/movement_controller/movement_controller/enums/circ_type_enum.py — confirm CircTypeEnum values
    - src/movement_controller/movement_controller/ur_movement_controller.py — confirm the existing
      except (ValidationError, ValueError) block in _goal_callback that will catch the new ValueError
  </read_first>
  <action>
    In TrajectoryPathDTO.from_ros_msg, replace the current circ_type resolution block:

    REMOVE this block:
      try:
          if ros_msg.circ_type == '' or ros_msg.circ_type is None:
              circ_type = CircTypeEnum.INTERIM
          else:
              circ_type = CircTypeEnum(ros_msg.circ_type)
      except ValueError:
          raise ValueError(f'Invalid circ_type: {ros_msg.circ_type!r}')

    REPLACE with:
      if motion_type == MotionTypeEnum.CIRC:
          if not ros_msg.circ_type:
              raise ValueError(
                  f'path {ros_msg.path_id!r} has motion_type CIRC but circ_type is empty; '
                  f'must be "interim" or "center"'
              )
          try:
              circ_type = CircTypeEnum(ros_msg.circ_type)
          except ValueError:
              raise ValueError(
                  f'path {ros_msg.path_id!r} has motion_type CIRC but invalid circ_type '
                  f'{ros_msg.circ_type!r}; must be "interim" or "center"'
              )
      else:
          circ_type = CircTypeEnum(ros_msg.circ_type) if ros_msg.circ_type else CircTypeEnum.INTERIM

    Also add 'from movement_controller.enums.motion_type_enum import MotionTypeEnum' to the imports
    if it is not already present in this file.

    Do NOT modify _goal_callback — the existing except (ValidationError, ValueError) → REJECT
    block already handles the new ValueError correctly.

    Also update src/movement_controller/tests/unit/test_enums_and_dtos.py:
      - Run existing tests first to confirm they still pass (direct DTO construction is unaffected).
      - Add new test functions for from_ros_msg covering the changed behaviour:
          test_from_ros_msg_circ_empty_circ_type_raises — CIRC path with circ_type='' raises ValueError
          test_from_ros_msg_circ_invalid_circ_type_raises — CIRC path with circ_type='bad' raises ValueError
          test_from_ros_msg_circ_valid_interim — CIRC path with circ_type='interim' succeeds
          test_from_ros_msg_circ_valid_center — CIRC path with circ_type='center' succeeds
          test_from_ros_msg_non_circ_empty_circ_type_defaults_to_interim — LIN path with circ_type=''
            still defaults to CircTypeEnum.INTERIM (non-CIRC paths unaffected)
      Each test should build a minimal mock TrajectoryPath ROS message (using MagicMock or a
      real TrajectoryPath() instance) and call TrajectoryPathDTO.from_ros_msg(msg).
  </action>
  <verify>
    <automated>grep -n "motion_type == MotionTypeEnum.CIRC\|circ_type is empty" /workspaces/movement_controller/src/movement_controller/movement_controller/models/trajectory_path_dto.py</automated>
    <automated>cd /workspaces/movement_controller && source /opt/ros/jazzy/setup.bash && source install/setup.bash && python -m pytest src/movement_controller/tests/unit/test_enums_and_dtos.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - from_ros_msg no longer contains 'CircTypeEnum.INTERIM' as a silent default for empty circ_type
    - from_ros_msg raises ValueError when motion_type==CIRC and circ_type is empty (falsy)
    - ValueError message includes the path_id and describes the requirement ('must be "interim" or "center"')
    - from_ros_msg raises ValueError when motion_type==CIRC and circ_type is an unrecognized string
    - from_ros_msg still accepts empty circ_type for non-CIRC paths (defaults to CircTypeEnum.INTERIM)
    - from_ros_msg still accepts 'interim' and 'center' for CIRC paths without error
    - _goal_callback is NOT modified (no new code added)
    - All existing tests in test_enums_and_dtos.py still pass (direct DTO construction is unaffected)
    - 5 new from_ros_msg tests added to test_enums_and_dtos.py and all pass
  </acceptance_criteria>
  <done>
    TrajectoryPathDTO.from_ros_msg raises ValueError for CIRC paths with missing or invalid
    circ_type. _goal_callback's existing except block catches this and returns REJECT.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace stub _execute_callback with PILZ plan+execute loop</name>
  <files>src/movement_controller/movement_controller/ur_movement_controller.py</files>
  <read_first>
    - src/movement_controller/movement_controller/ur_movement_controller.py — read current
      _execute_callback (lines ~133–175) in full to understand stub structure before replacing
    - .planning/phases/03-moveit2-pilz-single-path-execution/03-CONTEXT.md — D-13 (flatten groups),
      D-14 (stop-start intentional), D-15 (per-path feedback), D-16 (fail-fast on plan failure),
      D-17 (fail-fast on execution failure), D-18 (log ERROR before returning failure)
    - .planning/phases/03-moveit2-pilz-single-path-execution/03-RESEARCH.md — Pattern 4 (plan + execute
      result handling), Pitfall 4 (NO blocking kwarg on execute())
    - .planning/phases/03-moveit2-pilz-single-path-execution/03-PATTERNS.md — _execute_callback
      flatten + plan + execute pattern section
  </read_first>
  <action>
    Replace the inner body of _execute_callback (the loop that currently sends stub feedback per group)
    with the real flatten-groups → plan-each-path → execute-each-path loop. Keep the outer
    try/except/finally structure UNCHANGED — only the content inside the 'try' block changes.

    New try block content:
      goal_dto = TrajectoryGoalDTO.from_ros_msg(goal_handle.request)
      groups = TrajectoryGrouper.group(goal_dto.paths)
      completed_ids: list[str] = []

      for group in groups:
        for path in group:
          # Publish executing feedback per-path (D-15)
          fb = ExecuteTrajectory.Feedback()
          fb.status = FeedbackStatusEnum.EXECUTING.value
          fb.trajectory_path_ids = [path.path_id]
          goal_handle.publish_feedback(fb)

          # Plan via PILZ (D-16: fail-fast on planning failure)
          plan_result = self._planner_service.plan(path)
          if not plan_result.success:
            log ERROR: f'Planning failed for path {path.path_id!r}: {plan_result.error_message}'
            result = ExecuteTrajectory.Result()
            result.success = False
            result.error_message = plan_result.error_message
            goal_handle.abort()
            return result

          # Execute trajectory — NO blocking kwarg (Research Pitfall 4)
          exec_status = self._moveit.execute(plan_result.trajectory, controllers=[])
          if not exec_status:
            err = f'Execution failed for path {path.path_id!r}'
            log ERROR: err
            result = ExecuteTrajectory.Result()
            result.success = False
            result.error_message = err
            goal_handle.abort()
            return result

          # Publish completed feedback per-path (D-15)
          fb2 = ExecuteTrajectory.Feedback()
          fb2.status = FeedbackStatusEnum.COMPLETED.value
          fb2.trajectory_path_ids = [path.path_id]
          goal_handle.publish_feedback(fb2)
          completed_ids.append(path.path_id)

      result = ExecuteTrajectory.Result()
      result.success = True
      result.error_message = ''
      result.trajectory_paths_completed = completed_ids
      goal_handle.succeed()
      return result

    Keep the existing except Exception and finally blocks exactly as they are.
    No new imports are needed (PilzPlannerService import was added in Plan 03-01 Task 2).
  </action>
  <verify>
    <automated>grep -n "self._planner_service.plan\|self._moveit.execute\|completed_ids" /workspaces/movement_controller/src/movement_controller/movement_controller/ur_movement_controller.py</automated>
  </verify>
  <acceptance_criteria>
    - Source contains 'self._planner_service.plan(path)' inside _execute_callback
    - Source contains 'self._moveit.execute(plan_result.trajectory, controllers=[])' — exactly this
      signature with no 'blocking' keyword argument
    - Source contains 'completed_ids' list populated during the path loop
    - For each path: 'FeedbackStatusEnum.EXECUTING.value' appears before plan() call and
      'FeedbackStatusEnum.COMPLETED.value' appears after execute() call (maintaining per-path ordering)
    - Both planning failure and execution failure paths call 'goal_handle.abort()' and return a
      Result with success=False
    - Error logging (self.get_logger().error(...)) appears before each abort() call
    - The 'finally' block with '_is_executing = False' is still present (unchanged from Phase 2)
    - The 'except Exception' block with abort() is still present (unchanged from Phase 2)
    - Source does NOT contain 'blocking=True' anywhere in the execute() call
  </acceptance_criteria>
  <done>
    _execute_callback iterates flattened paths, plans each via PILZ, executes, sends per-path
    executing/completed feedback, aborts with error on first planning or execution failure,
    and returns success with all completed path IDs on full success.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS2 action goal → _goal_callback | circ_type string from untrusted caller validated before DTO conversion |
| _execute_callback → MoveItPy.execute() | trajectory object produced by PILZ planner; execution result checked for failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | circ_type in raw goal.paths | mitigate | from_ros_msg raises ValueError for CIRC paths with empty or unrecognized circ_type; _goal_callback's existing except block returns REJECT before any planning or DTO is stored |
| T-03-03-02 | Denial of Service | _is_executing flag not released on CIRC reject | accept | _is_executing is set in _goal_callback only after all validation passes (on ACCEPT, step 4). CIRC ValueError fires at step 3 (from_ros_msg validation), before the flag is ever set. DoS is structurally impossible on this path — no release needed. |
| T-03-03-03 | Denial of Service | _execute_callback planning infinite block | accept | PILZ planning_time=5.0s per PlanRequestParameters in PilzPlannerService; each path bounded |
| T-03-03-04 | Repudiation | Planning or execution failure without trace | mitigate | self.get_logger().error() called before every abort(); path_id included in all error messages |
</threat_model>

<verification>
- `grep 'self._planner_service.plan' src/movement_controller/movement_controller/ur_movement_controller.py` exits 0
- `grep "self._moveit.execute(plan_result.trajectory, controllers=\[\])" src/movement_controller/movement_controller/ur_movement_controller.py` exits 0
- `grep 'motion_type == MotionTypeEnum.CIRC' src/movement_controller/movement_controller/models/trajectory_path_dto.py` exits 0
- `grep 'circ_type is empty' src/movement_controller/movement_controller/models/trajectory_path_dto.py` exits 0
- Confirm no silent INTERIM default for CIRC: `grep 'CircTypeEnum.INTERIM' src/movement_controller/movement_controller/models/trajectory_path_dto.py` only appears in the else (non-CIRC) branch
- Confirm no 'blocking=True' in the execute() call: `grep 'blocking' src/movement_controller/movement_controller/ur_movement_controller.py` returns nothing
</verification>

<success_criteria>
- CIRC path with empty or unrecognized circ_type raises ValueError in from_ros_msg; caught by _goal_callback existing except block → REJECT
- _execute_callback iterates individual paths (not groups); per-path executing+completed feedback
- Planning failure cause goal abort with path_id in error_message
- Execution failure causes goal abort with path_id in error_message
- moveit.execute() called without blocking kwarg
</success_criteria>

<output>
Create `.planning/phases/03-moveit2-pilz-single-path-execution/03-03-SUMMARY.md` when done.
</output>
