---
phase: "03-moveit2-pilz-single-path-execution"
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/movement_controller/tests/integration/test_moveit_execution.py
autonomous: true
requirements:
  - MOT-01
  - MOT-02
  - MOT-05

must_haves:
  truths:
    - "1-path LIN goal → result.success=True, trajectory_paths_completed=[path_id], 2 feedback calls"
    - "Planning failure → result.success=False, error_message contains path_id, goal_handle.abort() called"
    - "Execution failure → result.success=False, error_message contains path_id, goal_handle.abort() called"
    - "CIRC goal with valid circ_type='interim' → result.success=True"
    - "Concurrent goal rejected (MOT-05): _goal_callback returns REJECT when _is_executing=True"
    - "CIRC path with circ_type='' → _goal_callback returns REJECT (D-11)"
    - "All assertions pass with MoveItPy fully mocked at module level (no ros-jazzy-moveit needed)"
  artifacts:
    - path: "src/movement_controller/tests/integration/test_moveit_execution.py"
      provides: "Integration smoke test for full wired controller with mocked MoveItPy"
      contains: "test_execute_trajectory_single_path_success"
  key_links:
    - from: "test fixtures"
      to: "URMovementController._execute_callback"
      via: "asyncio.run(_execute_callback(mock_goal_handle))"
      pattern: "asyncio.run\\(.*_execute_callback"
    - from: "node_with_moveit fixture"
      to: "MoveItPy mock"
      via: "patch('movement_controller.ur_movement_controller.MoveItPy')"
      pattern: "patch.*ur_movement_controller.MoveItPy"
---

<objective>
Write an integration smoke test that exercises the fully-wired `URMovementController`
with `MoveItPy` mocked at the Python module level. The test verifies that a 1-path goal
produces the correct feedback sequence and result, and that planning/execution failures
are properly handled.

Per D-19: no real `move_group` node is required. This test can run in `colcon test`
without a robot.

Purpose: Proves the Plans 01–03 integration is correct end-to-end and provides a
regression gate for Phase 4 changes.

Output: `tests/integration/test_moveit_execution.py` with all tests passing.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-moveit2-pilz-single-path-execution/03-CONTEXT.md
@.planning/phases/03-moveit2-pilz-single-path-execution/03-RESEARCH.md
@.planning/phases/03-moveit2-pilz-single-path-execution/03-PATTERNS.md
</context>

<interfaces>
<!-- Key patterns the executor must follow for the integration test. -->

Existing test patterns (test_ur_movement_controller.py):
  @pytest.fixture(scope='module')
  def ros_context():
      rclpy.init()
      yield
      rclpy.shutdown()

  @pytest.fixture
  def node(ros_context):
      n = URMovementController()
      yield n
      n.destroy_node()

  _make_ros_goal helper — returns a MagicMock with:
    .paths = [MagicMock with path_id, motion_type, blend_radius, target_pose,
              circ_point, cartesian_speed, acceleration, tool_frame, circ_type]

For integration test, node fixture must:
  1. patch 'movement_controller.ur_movement_controller.MoveItPy' before creating URMovementController
  2. Set n._is_active = True (simulate activated node)
  3. Set n._moveit = the mock moveit instance
  4. Set n._planner_service as a MagicMock with plan.return_value having success=True
     and trajectory=MagicMock()

Goal handle pattern (from test_ur_movement_controller.py lines 90–115):
  mock_goal_handle = MagicMock()
  mock_goal_handle.request.paths = [_make_path_msg(path_id)]
  mock_goal_handle.publish_feedback = MagicMock()
  mock_goal_handle.succeed = MagicMock()
  mock_goal_handle.abort = MagicMock()
  result = asyncio.run(node_with_moveit._execute_callback(mock_goal_handle))

Pattern for testing _goal_callback CIRC rejection (D-11):
  goal = MagicMock()
  path = MagicMock()
  path.path_id = _UUID1
  path.motion_type = 'CIRC'
  path.circ_type = ''   # empty — should be rejected
  goal.paths = [path]
  result = node_with_moveit._goal_callback(goal)
  assert result == GoalResponse.REJECT

Pattern for testing concurrent rejection (MOT-05):
  node_with_moveit._is_executing = True
  result = node_with_moveit._goal_callback(valid_goal)
  assert result == GoalResponse.REJECT
  node_with_moveit._is_executing = False  # clean up
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write integration smoke test</name>
  <files>src/movement_controller/tests/integration/test_moveit_execution.py</files>
  <read_first>
    - src/movement_controller/tests/unit/test_ur_movement_controller.py — full file; BSD-3-Clause
      header, ros_context fixture, node fixture, _make_ros_goal helper, asyncio.run pattern for
      async callbacks, MagicMock goal handle structure
    - src/movement_controller/tests/integration/ — check if any existing files are present
    - .planning/phases/03-moveit2-pilz-single-path-execution/03-CONTEXT.md — D-15 (feedback sequence
      per path: executing → completed), D-16 (fail-fast abort on plan failure), D-17 (abort on
      execution failure), D-19 (mock at Python level, no real move_group)
    - .planning/phases/03-moveit2-pilz-single-path-execution/03-PATTERNS.md — integration test section
      (node_with_moveit fixture, core integration test, abort test patterns)
  </read_first>
  <action>
    Create the test file with BSD-3-Clause header and module docstring
    'Integration smoke test — URMovementController with MoveItPy mocked at module level.'

    Imports:
      import asyncio
      from unittest.mock import MagicMock, patch
      import pytest
      import rclpy
      from geometry_msgs.msg import Point, PoseStamped
      from rclpy.action import GoalResponse
      from movement_controller.ur_movement_controller import URMovementController

    Module-level UUID constant: _UUID1 = '00000000-0000-4000-8000-000000000001'

    Helper _make_path_msg(path_id, motion_type='LIN', circ_type='interim') → MagicMock:
      Attributes: path_id, motion_type, blend_radius=0.0, target_pose=PoseStamped(),
      circ_point=Point(), cartesian_speed=0.0, acceleration=0.0, tool_frame='', circ_type.

    Fixtures:
      ros_context (scope='module'): rclpy.init(); yield; rclpy.shutdown()
      node_with_moveit (function scope, depends on ros_context):
        Build mock_moveit with mock plan result (truthy __bool__, .trajectory attribute).
        Build mock_arm = MagicMock() with plan.return_value = mock_plan (truthy).
        mock_moveit.get_planning_component.return_value = mock_arm
        mock_exec_status = MagicMock(__bool__=MagicMock(return_value=True))
        mock_moveit.execute.return_value = mock_exec_status
        Use patch('movement_controller.ur_movement_controller.MoveItPy', return_value=mock_moveit)
        as a context manager (with statement), create n = URMovementController() inside.
        Set n._is_active = True, n._moveit = mock_moveit.
        Set n._planner_service = MagicMock() with plan.return_value having .success=True
        and .trajectory=MagicMock().
        yield n; n.destroy_node() in cleanup.

    Tests to implement (8 tests):

    1. test_execute_trajectory_single_path_success:
       Mock goal_handle with 1-path LIN goal; run _execute_callback; assert result.success=True,
       result.trajectory_paths_completed==[_UUID1], publish_feedback.call_count==2, succeed called once.

    2. test_execute_trajectory_feedback_order:
       Capture publish_feedback call args; assert first call status == 'executing',
       second call status == 'completed' (D-15 per-path ordering).

    3. test_execute_trajectory_aborts_on_plan_failure:
       Set node._planner_service.plan.return_value to MagicMock(success=False,
       error_message=f'PILZ LIN planning failed for path {_UUID1!r}').
       Run _execute_callback; assert result.success=False, _UUID1 in result.error_message,
       goal_handle.abort called once, succeed not called.

    4. test_execute_trajectory_aborts_on_execution_failure:
       Set node._moveit.execute.return_value to MagicMock(__bool__=MagicMock(return_value=False)).
       Run _execute_callback; assert result.success=False, _UUID1 in result.error_message,
       goal_handle.abort called once.

    5. test_execute_trajectory_circ_path_success:
       1-path CIRC goal with circ_type='interim'; assert result.success=True.
       (D-15: CIRC paths behave identically to LIN/PTP in execution callbacks)

    6. test_goal_rejects_circ_with_empty_circ_type (D-11):
       Set node._is_active=True, node._is_executing=False.
       Build goal with a CIRC path where circ_type=''.
       Call _goal_callback(goal); assert result == GoalResponse.REJECT.
       Assert node._is_executing is False (flag released on reject).

    7. test_goal_rejects_concurrent_execution (MOT-05):
       Set node._is_active=True, node._is_executing=True.
       Assert _goal_callback(valid_goal) == GoalResponse.REJECT.
       Clean up: node._is_executing=False.

    8. test_goal_rejects_circ_with_unrecognized_circ_type:
       Same as test 6 but circ_type='unknown'. Assert REJECT.

    For tests 6–8 that test _goal_callback: create a simple helper _make_goal_with_circ_path
    that returns a MagicMock goal with one CIRC path having the given circ_type string.

    After each test that modifies node._is_executing, reset it to False to prevent test leakage.
    Use try/finally or direct reset in the test body (not shared fixtures) for this cleanup.
  </action>
  <verify>
    <automated>cd /workspaces/movement_controller && source /opt/ros/jazzy/setup.bash && source install/setup.bash && python -m pytest src/movement_controller/tests/integration/test_moveit_execution.py -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File begins with BSD-3-Clause copyright block
    - File contains exactly 8 test functions (test_execute_* and test_goal_*)
    - pytest reports all 8 tests PASSED (0 failures, 0 errors)
    - test_execute_trajectory_feedback_order captures feedback call args and verifies status ordering:
      first call == 'executing', second call == 'completed'
    - test_execute_trajectory_aborts_on_plan_failure asserts result.success is False AND
      _UUID1 appears in result.error_message AND goal_handle.abort.assert_called_once()
    - test_goal_rejects_circ_with_empty_circ_type asserts GoalResponse.REJECT AND
      node._is_executing is False after the callback (flag released)
    - test_goal_rejects_concurrent_execution asserts GoalResponse.REJECT (MOT-05)
    - No test imports from moveit directly; all moveit interactions are via mocks
    - Each test is independent: no shared mutable state leaks between tests
  </acceptance_criteria>
  <done>
    All 8 integration tests pass. End-to-end flow verified: 1-path success, feedback order,
    plan failure abort, execution failure abort, CIRC success, CIRC circ_type validation,
    concurrent rejection.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → mock_moveit | Test injects mock at module level; verifies controller handles both truthy and falsy execution results |
| _goal_callback → raw goal.paths | Tests verify circ_type string validation on untrusted input before DTO conversion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Repudiation | Plan/execution failure not observable in test | mitigate | test_execute_trajectory_aborts_on_plan_failure and _on_execution_failure verify abort() called and error_message identifies failing path_id |
| T-03-04-02 | Denial of Service | _is_executing flag leaked between tests | mitigate | Tests that set _is_executing=True clean up in finally/direct reset; test isolation enforced |
| T-03-04-03 | Tampering | circ_type empty string bypasses CIRC validation | mitigate | test_goal_rejects_circ_with_empty_circ_type explicitly proves empty string → REJECT |
</threat_model>

<verification>
- `python -m pytest src/movement_controller/tests/integration/test_moveit_execution.py -v` all PASSED
- `grep -c 'def test_' src/movement_controller/tests/integration/test_moveit_execution.py` outputs 8
- `grep 'GoalResponse.REJECT' src/movement_controller/tests/integration/test_moveit_execution.py` exits 0
- `grep 'goal_handle.abort' src/movement_controller/tests/integration/test_moveit_execution.py` exits 0
</verification>

<success_criteria>
- 8 integration tests all pass with MoveItPy mocked at module level
- 1-path LIN success verified: result.success=True, 2 feedback messages (executing/completed), trajectory_paths_completed=[_UUID1]
- Planning failure verified: abort called, success=False, path_id in error_message
- Execution failure verified: abort called, success=False, path_id in error_message
- CIRC with empty circ_type verified: GoalResponse.REJECT, _is_executing released
- Concurrent goal rejection verified: GoalResponse.REJECT when _is_executing=True
</success_criteria>

<output>
Create `.planning/phases/03-moveit2-pilz-single-path-execution/03-04-SUMMARY.md` when done.
</output>
